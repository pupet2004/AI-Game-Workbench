# 我们为什么要做 Workbench

Workbench 从一开始就不是为了再造一个更强的 AI Agent，也不是为了把所有 AI 能力都塞进一个平台。

我们真正想解决的问题其实更简单：

> 当一个项目持续几个月、几年，期间使用的模型、Agent、会话和工具不断变化时，怎样让项目本身不要跟着这些参与者一起失忆？

现在的大模型已经很强，专业 Agent 也越来越强。Codex 擅长代码，其他模型可能更擅长讨论、研究、设计、审查或多模态任务。未来还会不断出现新的模型、新的 Agent 和新的运行方式。

因此，Workbench 没有必要和这些系统竞争“谁更会写代码”“谁更会调用工具”“谁的 Agent Loop 更聪明”。

我们真正需要补上的，是它们之间目前仍然比较薄弱的一层：

**项目如何长期存在，如何把不同 Agent 串起来，如何知道什么已经决定，如何把工作交给下一个参与者，以及换掉当前模型之后，项目还能不能继续。**

论文里把这一点定义为项目连续性层：Workbench 位于 Agent、知识库、Git 等现有工具之间，不替代它们，而是让项目身份、决策来源和接受状态不依赖任何一个具体 AI。

## 一、为什么 Leader 和 Worker 必须分开

一个长期项目里，并不存在一个模型永远最适合所有事情。

讨论架构时，我们可能希望使用推理能力更强的模型；真正写代码时，专业 Coding Agent 往往更合适；整理资料、生成摘要或者进行简单检查时，又没有必要一直调用最昂贵、最强的模型。

所以我们不希望设计一个“万能 Agent”。

Workbench 采用 Leader 和 Worker 的分离：

**Leader 负责理解项目、理解用户意图、拆分任务和协调工作；Worker 负责完成具体任务。**

Leader 更像项目中的协调者。它知道项目现在是什么状态、什么已经确定、当前正在解决什么问题，然后决定下一项工作应该交给谁。

Worker 则可以是 Codex，也可以是其他 Coding Agent，可以是普通 LLM，可以是本地模型，甚至未来可以是专门处理图片、音频、测试或研究的 Agent。

论文中的设计本身也基于这个判断：架构推理、代码实现和常规整理需要的能力不同，因此 Leader 应把任务路由给更合适的 Worker，而不是要求同一个 Agent 承担全部职责。

这样做还有一个很现实的好处：

**我们可以按任务使用能力，而不是按项目绑定模型。**

简单任务不必浪费高级模型，高风险任务也不用为了省一点调用成本而交给能力不足的模型。

模型因此成为一种可以选择和调度的资源，而不是项目本身。

---

## 二、为什么 Leader 和 Worker 都应该能够更换

这是 Workbench 很重要的一条原则。

我们不认为今天使用的模型，会一直是几年之后最好的模型。

甚至不能保证它几年以后还存在。

过去几年已经证明，大模型和 Agent 的更新速度远远快于正常项目的生命周期。模型会升级、改名、下线；Agent 会改变接口；新的厂商会出现；原本领先的产品也可能被新的系统替代。

如果一个项目的长期记忆、任务结构和决策全部绑定在某一个模型里，那么换模型实际上就接近于换项目大脑。

这正是我们希望避免的事情。

因此 Workbench 的目标不是：

> “让某个 Agent 永远记住这个项目。”

而是：

> “无论今天接入的是谁，它都能够重新进入这个项目。”

论文将 Agent 可替换性直接作为一等设计目标：不同 Agent 接收统一的任务和上下文，并以统一形式返回结果与 Handoff；模型变化只应影响适配层，而不应该影响项目核心状态。

这也是为什么我们现在要测试 Codex 之外的 LLM 和 Agent。

这不是为了单纯增加一个“模型列表”。

真正需要验证的是：

**Workbench 是否真的与 Codex 无关。**

如果换成另外一个 LLM、另外一个 Agent、另外一种 Session 机制以后，项目仍然能够完成：

**读取项目状态 → 接收任务 → 执行 → 返回结果 → 交接 → 继续推进**

那么可替换性才真正成立。

---

## 三、为什么要做 Agent 路由，而不是把所有能力自己实现一遍

因为没有必要。

现在的大模型厂商和 Agent 产品已经投入了大量资源解决执行问题。

Codex 会自己理解代码库、调用工具、修改代码和执行测试；其他 Agent 也会发展自己的规划、搜索、浏览器、Shell、IDE、多模态和工具调用能力。

Workbench 很难，也没有必要重新实现一套这些东西。

即使今天重新实现了，半年以后也很可能落后。

因此我们的思路是：

> 别人把 Agent 做强，我们负责让这些 Agent 能够进入同一个项目世界。

Workbench 做的是一层相对薄的协调层和连续性层。

对于外部 Agent，我们真正关心的只有几件事：

它接到了什么任务；

它工作时应该知道哪些项目状态；

它做出了什么结果；

这个结果属于建议、证据还是已经接受的项目事实；

它结束以后，需要给下一个参与者留下什么。

至于这个 Agent 内部到底用了多少轮推理、什么工具链、什么上下文管理方式，原则上不应该由 Workbench 管理。

论文对此的边界也很明确：Workbench 不创建新的 Agent Runtime，而是让已有 Agent 在项目连续性和治理约束下被调度；适配器只是连接器，而不是治理者。

这实际上也是一个非常现实的工程选择。

**我们不和模型公司竞争 Agent 能力，而是利用模型公司持续提升的 Agent 能力。**

它们越强，Workbench 反而越有价值，因为我们需要负责的问题始终是：

**这些越来越强、越来越多、越来越容易替换的参与者，怎样共同维护同一个长期项目。**

---

## 四、为什么需要 Handoff 和 Session 路由

因为一次会话不是一个项目。

无论上下文窗口变得多大，会话仍然会结束。

而且长期使用同一个 Session 本身也未必合理：上下文越来越长以后，会出现噪声、旧信息干扰、成本增加和重点稀释。

所以我们不能把项目连续性建立在“永远不要开新聊天”上。

Workbench 更希望允许 Session 正常死亡。

一个 Session 完成阶段性任务后，只需要留下足够高密度的 Handoff。新的 Session 或新的 Agent 可以读取项目当前状态，再读取这份交接，然后继续工作。

因此：

**Session 是临时工作空间，项目才是长期存在的东西。**

这也是路由存在的另一个意义。

用户不需要自己记住：

“这个任务之前在哪个聊天做的？”

“下一次是不是必须继续使用那个模型？”

“这个 Worker 的上下文还剩多少？”

Workbench 应该知道当前任务属于哪个 Attempt、哪个 Session、由哪个 Agent 承担；当参与者需要更换时，再把工作接到新的 Session 上。

换掉的是执行现场，不是项目本身。

---

## 五、为什么项目状态不能直接等于聊天记录

因为聊天里有大量东西并不是真的决定。

我们会提出方案，会推翻方案，会说“也许可以这样”，会让 Agent 做实验，也会得到错误答案。

如果把所有聊天记录都当成项目记忆，那么未来的 Agent 能知道“以前说过什么”，却很难判断：

**到底哪一个才是真的？**

所以 Workbench 必须区分：

“有人提出过这个东西”，和“项目已经接受这个东西”。

这就是 Claim、Authority Decision 和 Accepted Project State 存在的理由。

Agent 可以自由思考，可以提出建议，也可以犯错。

但 Agent 的一句话不能因为被写进聊天记录，就自动变成项目事实。

论文将这一点作为核心治理边界：Agent 输出先成为 Claim，只有经过 Authority Decision，才能进入 Accepted Project State。

这看起来增加了一层结构，但它解决的是长期项目里非常实际的问题：

**项目最终需要一个地方回答——“我们现在到底认为什么是真的？”**

---

## 六、为什么要做多层信息，而不是每次把整个项目重新喂给 AI

因为长期项目真正昂贵的，并不仅仅是一次调用用了多少 Token。

更大的浪费是：

**同一个项目，被不同 Agent 一遍又一遍重新理解。**

如果每次开新 Session，都把几个月的聊天记录、所有设计文档、所有代码历史重新塞进去，那么项目越长，成本越高，而且大量信息与当前任务根本没有关系。

因此 Workbench 希望信息逐渐压缩：

Session 保留原始工作过程；

Handoff 保存一次工作的交接信息；

Summary 保存高密度的重要变化；

Library 提供长期可浏览的项目演进；

Accepted Project State 保存当前真正需要被视为项目现实的内容。

新的参与者优先读取最高密度、足够完成当前任务的一层；只有发现信息不足时，才继续向下查来源。

论文把这一原则称为“最小充分上下文”：简单任务读取接受状态，复杂任务再下钻到决策、Handoff 和原始 Session。

所以我们追求的不是：

**让 AI 记住更多。**

而是：

**让 AI 没有必要每次重新知道所有东西。**

---

## 七、为什么还需要“项目级检索”，而不是只把历史保存下来

因为信息被保存下来，并不等于用户以后还能真正找到它。

长期使用 AI 以后，一个项目的思考过程很容易散落在很多地方：

ChatGPT 的多个 Session；

Codex 的不同 Thread；

其他 LLM 或 Agent 的会话；

本地运行日志；

Markdown、Word、Git、截图；

以及手机备忘录、随笔或临时记录。

这些信息中的大部分其实并没有消失。

真正的问题是：

**用户已经不知道它在哪里。**

人对自己的记忆，并不是一个可靠的项目索引。

几个月以后，我们经常只会记得：

“这个问题以前好像讨论过。”

“我记得当时想到过一个很好的方案。”

“Boss 这里是不是已经决定过为什么要这样做？”

但我们未必还能记得：

是在哪个模型里讨论的；

是哪一个 Session；

用的是什么关键词；

后来有没有修改；

最后到底采用了哪个版本。

于是用户只能在大量互相隔离的会话和文件之间反复翻找。

更麻烦的是，即使某个平台保存了几百个 Session，用户通常也未必拥有像在 Word、IDE 或本地文件中那样，对整个历史语料进行完整搜索和重新组织的能力。

用户真正需要的并不只是“历史记录还在”。

而是能够回答：

>我以前在哪里讨论过 Boss？

然后继续回答：

>哪些只是随口提出的方案？

>哪些后来被正式采用？
 >哪些已经被新的决定取代？
 >如果我要继续这个问题，应该先读哪一份？
 >如果我想看当时的原话，又应该从哪里回到 Source 和 Session？

所以 Workbench 不应该只做一个更大的聊天记录仓库。

它需要建立的是一个**项目级统一索引**。

无论信息最初来自 GPT、Codex、其他 Agent、本地文件还是用户自己的笔记，只要它进入这个项目世界，就应该尽可能知道：

>它属于哪个项目；

>什么时候产生；

>来源在哪里；

>由谁产生；

>讨论的是什么；

>后来有没有影响项目；

>现在是否仍然有效；

>以及如何回到原始来源。

这样，当用户以后搜索“Boss”时，理想结果不应该只是返回几百个包含这个词的文本片段。

Workbench 应该优先告诉用户：

**Current Truth：现在项目关于 Boss 的正式结论是什么；**

**Library：这个设计过去发生过哪些重要变化；**

**Summary：哪些阶段性总结与这个问题有关；**

**Source / Session：原始讨论具体发生在哪里。**

只有当用户真的需要考古时，才继续下钻到全部原始记录。

这和前面的“最小充分上下文”实际上是同一套原则：

**先回答用户真正想知道的，再帮助用户找到答案从哪里来。**

因此 Workbench 要解决的不只是“AI 会不会失忆”。

它还要解决另一种更常见的失忆：

> 信息其实还在，但用户已经失去了定位它的能力。

从这个角度看，Workbench 不是替用户记住所有事情。

更准确地说，它是在让用户不必承担“记住所有东西放在哪里”的责任。

人的记忆应该负责产生新的想法、判断什么重要、决定项目往哪里走。

机器更适合负责长期维护那些机械但昂贵的事情：

**索引、定位、关联、状态、来源和演进关系。**

最终我们希望把用户面对项目时的问题，从：

> “我到底把它写在哪儿了？”

变成：

> “我现在想知道什么？”

---

## 八、为什么 Workbench 必须轻量化

因为 Workbench 本质上应该是一层，而不是另一套基础设施。

如果为了获得项目连续性，用户首先要：

部署服务器；

配置数据库；

安装复杂运行时；

申请多个云服务；

理解一整套 Agent Framework；

配置几十个环境变量；

然后才能打开项目，

那这个产品本身就在制造新的维护成本。

因此我们希望 Workbench 尽量做到：

**下载以后能运行，创建或导入项目以后就能使用。**

用户可以继续使用自己原本的 Git、IDE、Agent 和项目目录。

Workbench 只是附着在现有工作流旁边。

论文中的边界也是如此：Workbench 刻意不成为综合 AI 平台，不实现自己的 Agent Runtime、代码执行环境、云基础设施和知识管理系统，而只专注于项目身份、决策治理、状态连续性与可追溯性。

轻量不是因为我们做不起更大的系统。

而是因为：

> 如果一个问题可以由薄层解决，就没有理由逼用户安装一个生态系统。

---

## 九、为什么强调本地优先

因为项目连续性本身就不应该依赖另一个可能消失的平台。

如果一个项目所有关键状态都只能存在某个云服务中，那么这个云服务一旦停止运营、改变收费方式、修改 API 或停止支持，所谓“长期连续性”本身就出现了矛盾。

因此项目最核心的数据应该尽量掌握在用户手里。

这也是我们选择类似 SQLite、Git 这种思路的重要原因：

它们普通、成熟、自包含，而且用户知道自己的数据在哪里。

Workbench 可以接入云端 Agent，但项目本身不应该成为某一家云 Agent 的附属物。

论文也明确把本地优先和项目持久性联系起来：核心项目状态应驻留在用户处，而不是依赖可能被弃用的云服务。

---

## 十、为什么还要让 Workbench 容易被“魔改”

这是轻量化之后更进一步的目标。

Workbench 面对的项目类型不会只有软件开发。

游戏项目、研究项目、内容项目、写作项目、个人长期计划，它们需要的 Worker 类型、项目字段、治理方式和界面重点都可能不同。

我们不可能提前设计出适合所有人的完美工作台。

而 Agent 时代出现了一个以前很难成立的新可能：

**软件本身可以被用户自己的 Agent 修改。**

因此 Workbench 如果保持代码结构清晰、模块边界明确、部署简单、数据结构容易理解，那么用户完全可以把自己的 Workbench 项目交给自己的 Coding Agent，然后说：

“我的项目不需要这个模块，删掉。”

“我主要做游戏，把这一块改成资产审查。”

“我使用另一个模型提供商，帮我接进去。”

“这个按钮不符合我的工作流，重新做。”

过去，“高度可定制软件”往往意味着我们自己要实现庞大的插件系统、脚本系统和配置中心。

现在有了 Coding Agent，另一种可能是：

> 我们提供一个足够小、足够清晰、足够稳定的基础，然后允许用户直接修改它。

所以易魔改不是一个附带特点。

它和轻量化是同一套设计思想：

**不要试图预判所有用户，而是降低用户改变软件的成本。**

---

## 十一、为什么尽量少配置

因为模型路由的价值不应该被配置成本抵消。

理论上，我们当然可以给用户提供几十个参数：

每种任务使用哪个模型；

上下文窗口多长；

Worker 最大运行次数；

什么时候换 Session；

什么时候压缩；

什么时候调用高推理模型；

什么时候自动审核。

但普通用户并不应该先理解整个架构才能使用 Workbench。

更合理的方式应该是：

**系统提供一套能直接工作的默认值，用户有需要时再逐渐修改。**

模型接入也一样。

理想状态不是让用户填几十项参数，而是：

选择 Provider；

完成认证；

选择 Leader；

选择可用 Worker；

然后开始工作。

高级用户当然可以调整路由和策略，但高级能力不能成为基本使用的前置条件。

一个长期工具如果每次安装都需要大量配置，本身就很难真正成为项目基础设施。

---

## 十二、为什么不把 Workbench 做成 IDE、Notion、Jira、Agent Framework 的集合体

因为这些工具已经存在，而且已经做得很好。

Git 负责实现历史；

IDE 和 Coding Agent 负责修改代码；

知识库负责存资料；

项目管理工具负责追踪任务；

模型厂商负责提升模型能力；

Agent Framework 负责执行复杂任务。

Workbench 没有必要重新发明它们。

它真正应该回答的是另外几个问题：

**这个项目是什么？**

**现在什么是真的？**

**为什么变成这样？**

**这个决定是谁、基于什么作出的？**

**当前工作应该交给谁？**

**这个 Agent 离开以后，谁能继续？**

**如果以后发现决定错了，我们怎样找到来源并恢复？**

论文也明确限定了 Workbench 的边界：它不是 AI 聊天应用、通用 Agent、知识库、项目管理工具或代码执行平台；它治理的是这些系统工作以后留下的项目意义。

边界越清楚，系统才越有机会保持简单。

---

## 十三、最终我们真正想做的东西

如果把所有架构术语都去掉，Workbench 想做的其实是一件很朴素的事情：

> 让一个项目不属于某一个 AI。

但进一步说，它也不应该只存在于用户当下能够想起来的记忆里。

用户当然是项目的主人，但用户不应该为了重新进入自己的项目，还必须记得某个想法究竟藏在第几个 Session、哪个客户端、哪份文件或哪条手机随笔中。

所以 Workbench 希望同时做到两件事：

> 项目不属于某一个 AI。
> 项目也不应该只存在于用户的记忆里。

今天可以使用 GPT。

明天可以使用 Codex。

后天可能换成 Claude、Gemini、本地模型或者一个今天还不存在的 Agent。

Leader 可以换。

Worker 可以换。

Session 可以结束。

工具可以升级。

模型可以淘汰。

但项目仍然知道：

**自己是谁；**

**已经决定了什么；**

**为什么这么决定；**

**现在做到哪里；**

**下一步需要做什么；**

**上一位参与者留下了什么；**

**新的参与者应该从哪里继续。**

Workbench 并不试图成为这些 AI 中最聪明的一个。

它更像是这些不断更换的参与者共同工作的那张桌子。

人可以离开，Agent 可以更换，模型可以淘汰。

用户也可以忘记某个想法当初究竟写在哪里。

**桌上的项目还在那里。**

而且当用户再次回来时，他不需要先成为自己过去两年的考古学家。

Workbench 应该能够帮助他重新知道：

**现在什么是真的，过去发生过什么，为什么变成这样，以及原始内容在哪里。**

这就是我们做 Workbench 的原因。
