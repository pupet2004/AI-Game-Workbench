# Workbench：面向长期 AI 协作的项目连续性层架构

**状态：** Architecture Preview（架构预览）  
**版本：** 白皮书 v0.1.1-preview
**日期：** 2026-08-28
**首次公开：** 2026-08-27
**作者：** pupet  
**软件状态：** 正在运行的参考实现；实现工作区已有 Windows-first `v0.1.0-alpha.20260827` 候选版本，但更广泛的软件公开发布和可用性验证仍在进行中。

**实现快照：** 已实现项目优先的持久状态、提案 → 决策 → 接受状态治理、权威层 / 上下文层 / 执行层分离、Handoff 与重启恢复、Agent 参与适配器、证据溯源记录，以及 WEIQI³ 跨 Agent 接力实验；**2026-08-27 本地工作树快照中 1,022 / 1,022 项测试通过**。Agent 交互界面体验、更广泛的 Provider 验证以及公开的连续性 / Token 实验仍在进行中。本文不宣称已经完成大规模用户验证、证明 Token 节省、多人治理或生产级企业平台。

---

## 中文版前言

本文不是提出一个新的 Agent，也不是试图替代现有 AI 工具。本项目关注的问题是：当 AI 成为长期项目参与者后，项目如何保持自身连续性。本文提出的 Workbench 架构是一个项目连续性层，位于现有 Agent 框架、版本控制系统和知识库之间，负责治理项目的意义、决策来源和状态恢复。软件与游戏开发是首个实现领域，但不是架构边界。

---

## 摘要

当 AI Agent、模型、会话与工具随时间不断更替时，长期项目如何维持其自身的连贯存在？近期系统和实践正分别从记忆、上下文管理、溯源和治理等方面处理这一问题，但它们的重点与边界并不相同。本文提出 Workbench，一种面向长期 AI 协作的项目连续性层（Project Continuity Layer），旨在将项目的长期存在与 AI 参与者的短暂性进行分离。Workbench 的贡献包括：（1）三层信息架构——权威层（Authority Lane）、上下文层（Context Lane）与执行层（Execution Lane），设计用于防止 AI 输出自动成为项目事实；（2）提案（Claim）–决策（Authority Decision）–状态模型，管控哪些拟议变更进入项目接受状态（Accepted Project State）；（3）一组通过渐进披露关联的上下文表征——会话、Handoff、摘要层、投影库和接受状态，而非单一的权威阶梯。本文分析了 Workbench 与事件溯源（Event Sourcing）、命令查询职责分离（CQRS）、Git、ADR、项目记忆系统及项目管理系统的关系，将其定位为互补层而非现有工具的替代品。一个实现验证了架构核心，涵盖五个模块共 1,022 项测试全部通过，表明该设计构成一个可运行的系统基底而非纯粹的概念性贡献。本文讨论了治理成本权衡、已知失效模式以及项目治理与 AI 安全之间的边界。Workbench 占据一个特定的生态位：在参与者更替的条件下治理项目的意义与决策来源。

**关键词：** 项目连续性，AI Agent 协作，长期项目，决策治理，架构设计，Agent 可替换性，信息架构，事件溯源，提案–决策–状态模型

---

## 1\. 引言

### 1.1 从 AI 辅助到 AI 参与：范式转变

人工智能与软件工程的关系正在经历深刻变革。在 AI 辅助开发的早期阶段，代码补全系统\[1]、静态分析引擎和重构助手等工具仅作为外围辅助手段——虽有价值，但处于核心创意与工程过程的边缘。开发者始终是项目决策的唯一作者，AI 仅充当一种精密的自动补全机制，基于局部上下文预测下一个词元或建议下一行代码\[2]。

GPT-4\[3]、Claude\[4]、Gemini 等大语言模型（LLM）及其后续模型的出现，催生了一场显著的转变。AI 系统不再局限于预测下一个词元，它们开始参与架构推理\[5]、生成跨文件代码变更\[6]、开展研究分析\[7]、调试复杂系统、提出设计方案并产出影响项目方向的创造性制品。AutoGPT\[8]、ReAct\[9]、AutoGen\[10]、LangChain\[11]、CrewAI 等 Agent 框架进一步拓展了这一能力，使 AI 能够自主规划、执行工具调用、迭代处理复杂任务并与其他 Agent 协调。

这一转变改变了 AI 参与的性质。关键问题不再是"AI 能否完成此项任务？"，而是"当多个 AI 参与者跨越数月乃至数年持续参与一个项目时，项目本身如何保持连贯？"这一区分至关重要：能力关注的是 Agent 在单次交互中能做什么；连续性关注的是项目在所有交互、所有 Agent、所有会话的累积中会发生什么。

连续性问题已然存在于当前工作流中。开发者跨会话使用 Cursor、Copilot 和 Claude 时会经历上下文丢失与决策漂移。游戏设计师使用 AI 进行关卡设计时，必须在每次会话中重新解释项目约束。该问题与领域无关：只要 AI 参与跨越多次交互的持续工作，该问题就会出现。

本文提出 Workbench，一种解决此问题的项目连续性层。

### 1.2 长期 AI 项目中的连续性问题

连续性问题可以精确表述为：*在涉及 AI 参与者的长期项目中，随着参与者的更替，项目连贯性可能逐渐退化，因为现有的执行、记忆和存储工具并不总能提供一个独立于参与者的基底，用于维护项目身份、决策溯源与接受状态。*

该问题有三个结构性成因。其一，AI 会话在设计上就是短暂的：它们开始、完成任务、结束，不携带持久状态。其二，AI 模型是不稳定的参与者：它们在短于大多数项目周期的时间尺度上被弃用、升级和替换。其三，现有工具要么优化执行（Agent 框架），要么优化存储（知识库），但不优化治理——即决定什么构成项目现实。

其结果是一种系统性模式：每次参与者更替都会引入微小的上下文丢失风险。经过数月和数十次更替，这些微小风险累积为显著的项目漂移。

为使这一问题具体化，考虑一个展示长期 AI 辅助项目实际运行状况的场景（详细版本见第 2.2 节）。

一个游戏开发团队基于 Godot 引擎创建了一款名为 *JadeFixio* 的 Roguelike 游戏。在十八个月的时间跨度内，多个 AI 参与者贡献了工作。通过大量设计探索，团队确立了多阶段 Boss 战斗结构。后续的 Agent 由于无法访问此决策历史，提出了相互矛盾的方案。一个测试 Agent 检测到不一致之处，但无法判断其属于刻意设计还是漂移所致。原始模型被弃用；新 Agent 加入时无法获取现有决策背后的推理过程。

该场景反映了任何跨越数月且涉及多个 AI 参与者的项目的实际运行状况。会话结束，模型更替，对"项目为何如此"的理解随每次更替而消散。

### 1.3 现有方法及其局限性

现有解决方案仅触及该问题的局部，普遍未能覆盖整体：

**会话记忆系统**\[12, 19]保留聊天历史，但不区分探索与承诺。一段考虑并否决了三种方案的会话以同等权重保留全部三种，使未来的参与者无法得知哪种方案真正被采纳。

**知识库**\[13]（Notion、Obsidian、Wiki）有效地存储信息，但不治理哪些信息构成项目现实。它们优化的是信息检索，而非决策治理。知识库可能同时包含一份提出血量缩放 Boss 的设计文档和一份后续提出三阶段结构的文档，却没有机制指明哪种方案代表当前已接受的方向。

**Agent 框架**\[8, 9, 10, 11]优化任务执行，但假定项目上下文由外部维护。Agent 接收任务、执行任务、返回结果。框架不治理这些结果是否成为项目接受状态的一部分，抑或仅停留在未提交的建议层面。

**Git**\[14]以精细的精度追踪代码变更，但运行在实现层。一次提交记录了 `EnemyAI.cs` 被修改，但不记录*为何*选择三阶段 Boss 结构、考虑过哪些替代方案或哪些证据支撑了该决策。

**架构决策记录**（ADR）\[15]捕获决策，但需要人工维护纪律，且传统上仅覆盖架构级选择。在一个每周需要在游戏机制、叙事、美术和技术领域做出数十项设计决策的项目中，手动维护 ADR 变得不切实际。

### 1.4 本文贡献

本文做出以下贡献：

1. **问题形式化。** 我们阐明了面向长期 AI 协作的项目连续性问题，将其与 Agent 记忆、知识管理和版本控制中的相关问题加以区分。我们论证：AI 参与者的不稳定性——而非其能力局限——是持续 AI 辅助工作的核心挑战，且这种不稳定性需要一个新的架构层，而非对现有工具的渐进改进。
2. **三层信息架构。** 我们设计了一个包含权威层（治理项目接受为现实的内容）、上下文层（提供理解背景）和执行层（记录任务执行方式）的信息架构。此分离防止 AI 生成的输出自动成为项目事实，并建立了一个跨 Agent 迁移持续存在的治理边界。三层模型借鉴了成熟的关注点分离原则，但将其应用于一个新领域：项目意义的治理。
3. **提案–决策–状态形式化。** 我们引入一种形式化模型，其中 AI 输出被捕获为提案（Claim），经由明确的决策（Authority Decision）后方能进入项目接受状态。该模型提供了一种清晰的治理机制，兼容从完全人工审核到基于策略自动接受的不同自动化级别，同时维持一个结构性不变量：任何变更不经决策关口即不得进入项目接受状态。
4. **多层压缩框架。** 我们设计了一个跨越会话、Handoff（交接包）、摘要层（Summary）、投影库（Library）和接受状态的压缩模型，每个层级在权威性、成本和可追溯性方面具有明确的定义角色。该框架并非通过降低单次交互成本来提升长期词元效率，而是通过消除困扰传统方法的项目上下文反复再理解来实现。压缩模型包含治理约束，防止压缩制品获得凌驾于其来源之上的权威性。
5. **实现验证。** 我们报告了一个验证核心架构内核的实现，涵盖五个系统模块共 1,022 项测试全部通过，证明所提设计构成一个可运行的系统基底而非纯粹的概念性贡献。完整的架构验证——通过用户研究、多 Agent 实验和纵向案例研究——留作未来工作。

---

## 2\. 背景与激励场景

### 2.1 长期 AI 项目的生命周期特征

长期项目——无论是在软件开发、游戏设计、科学研究还是持续内容创作领域——具有一些区别于一次性 AI 交互的共同特征。理解这些特征对于设计能解决实际运营需求的连续性层至关重要。

**时间跨度。** 此类项目跨越数月乃至数年，远超任何单次会话的生命周期。下文描述的游戏项目 *JadeFixio* 展示了现实的开发时间线。研究项目可能跨越三到五年。大型软件系统可能持续活跃开发十年之久。时间维度带来多重挑战：参与者更替、技术演进以及决策的累积——这些决策必须随时间保持连贯。连续性的重要性通常随项目时长递增。

**参与者更替。** 参与项目的人类和 AI Agent 随时间变化。团队成员因组织调整而轮换。AI 模型被升级、弃用或被更优的替代品取代。工具和框架演进，有时破坏向后兼容性。每次更替都有丢失累积项目理解的风险。在足够长的时间跨度上，参与者更替的概率趋近于一。一个无法在参与者更替中存续的项目，也无法在时间中存续。

**累积决策。** 一个成熟项目不仅由其当前状态定义，还由产出当前状态的决策历史定义。理解*为何*战斗系统采用三阶段 Boss 结构——而非血量缩放方案——需要访问决策溯源：考虑过的替代方案、评估过的证据、确认过的权衡以及导向接受的推理过程。此溯源往往比决策本身更有价值，因为它使未来的参与者能够评估决策的前提是否仍然成立。没有溯源的决策是没有正当性的规则——而没有正当性的规则是脆弱的。

**多模态制品。** 长期项目产出多样化的输出：源代码、文档、设计规格、测试结果、视觉资源、音频文件、战略分析、会议记录和用户反馈。这些制品存在于不同格式中，由不同工具产出，需要不同形式的治理。代码变更可能由自动化测试治理；设计决策可能需要人工审核；战略转向可能需要组织批准。

**可逆性要求。** 在长时间跨度内，错误不可避免。将当前状态追溯到其源头决策——并安全地撤销或修改这些决策的能力对项目健康至关重要。

### 2.2 激励案例：*JadeFixio* 项目

为以具体案例说明连续性问题，本文使用 *JadeFixio*——一个真实的游戏开发项目，它也是 Workbench 设计的动机来源。这不是 Workbench 管理该项目的验证案例，而是其运行痛点驱动了本文所述架构决策的项目。

**项目：** *JadeFixio* 是一款基于 Godot 引擎开发的独立游戏，核心玩法为四川麻将 Roguelike，包含玉牌系统、遗物系统和 Boss 挑战设计。开发周期超过一年，多个 AI 参与者参与设计讨论、代码实现和审查。

**项目身份。** 项目身份在创建之初确立：一款将麻将策略与程序化进程相结合的 Roguelike 游戏，强调战术深度和重玩价值。这不是功能列表，而是指导后续所有决策的设计哲学。

**阶段一：设计（第 1–3 月）。** 项目创建者使用 AI Agent 辅助核心系统设计。通过大量对话，探索了多种 Boss 难度方案：纯数值增长、阶段机制变化和自适应难度。经过分析，选定多阶段 Boss 结构——每个阶段引入不同机制，而非单纯提升数值。其他决策（遗物系统设计、玉牌经济）遵循相同的探索-确认模式。

**阶段二：实现（第 4–8 月）。** 代码生成 Agent 接管。它不仅需要理解*构建什么*，还需要理解*为何*做出特定设计选择。若无法访问设计依据，Agent 可能重新引入已被否决的方案，或与既定设计哲学不一致地实现功能。

**阶段三：审查（第 9–12 月）。** 测试 Agent 发现平衡问题。Boss 战斗体验良好，但遗物进程与核心战斗哲学不一致。要诊断问题，Agent 需要了解原始设计意图和塑造当前实现的决策。

**阶段四：演化（第 13 月+）。** 原始 AI 模型被弃用。新 Agent 加入。核心问题：新参与者能否理解项目的当前状态、身份、历史和决策依据？

此场景概括了项目连续性问题。*JadeFixio* 中观察到的痛点——丢失的决策上下文、矛盾的 Agent 提案、无法追溯设计依据驱动了本文后续描述的架构设计。

```
第 0 月         第 3 月          第 8 月          第 12 月         第 18 月
   |                |                |                |                |
   v                v                v                v                v
项目身份        设计决策         实现 Agent       测试 Agent       Leader
确立            完成             更替                                  替换
   |                |                |                |                |
   └─── 项目身份持续存在 ────────────────────────────────────────────┘
                    |                               |
              三阶段 Boss                        发现不一致
              决策确立                            但溯源不可用

问题：项目能否在参与者更替中存续？
图 1. 含参与者迁移的项目生命周期。项目身份与决策跨 Agent
更替持续存在，但若无治理层，决策溯源在每次更替中丢失。
```

### 2.3 上下文丢失的复合成本

连续性问题不仅是不便，它具有影响项目质量、效率和连贯性的复合成本。当新的 AI Agent 加入项目时，它必须从可用制品中重建项目理解。若决策溯源已丢失，该 Agent 可能：

* **重新引入已被否决的方案。** 不知道血量缩放方案已被明确否决，Agent 可能将其作为改进方案提出，消耗用户时间去再次否决一个已解决的问题。
* **不一致地应用设计哲学。** 不理解项目作为强调玩家自主权的策略 RPG 的身份，Agent 可能实现优先便利性而非策略深度的机制，微妙地破坏项目的设计连贯性。
* **重复先前的探索。** 无法访问先前的分析，Agent 可能花费词元和时间评估已被考虑并否决的方案，浪费计算资源并延误进度。
* **误读现有制品。** 不理解某些选择的背景，Agent 可能将刻意的设计决策视为需要修正的错误，引入回退而非改进。
* **提出相互冲突的变更。** 不理解完整的决策历史，Agent 可能提出与既定决策逻辑不一致的变更，在项目内部制造矛盾。

上述每种失效都会削弱项目连贯性。经过数月和数十次 Agent 更替，累积效应可能十分严重：项目因上下文的渐次丢失而偏离原始愿景。

成本不仅是定性的。反复进行上下文再理解会消耗注意力、词元和时间，也可能引入新的不一致。其具体规模仍是未来需要测量的实证问题，本文不在此宣称定量结果。

---

## 3\. 相关工作与边界分析

本节考察现有技术和方法论中涉及项目连续性问题各个方面的部分，明确每种方法解决了什么以及差距何在。我们的目标不是贬低现有工具——它们在其擅长的领域表现出色——而是精确界定 Workbench 所占据的空间。

### 3.1 版本控制系统：Git

Git\[14]是管理软件制品时间演进最成功且最广泛采用的工具。它提供了一个分布式、不可变的代码变更记录，使开发者能够追踪修改、归属变更作者、创建并行开发分支、合并分歧的工作以及以任意粒度遍历历史。Git 回答的问题是：*什么被更改了？谁更改的？何时更改的？任意历史时刻的状态是什么？*

Git 的设计哲学——不可变的内容寻址存储、分布式架构、轻量级分支——已被证明具有卓越的持久性，并影响了众多后续系统。其成功表明了将历史视为头等关注点的价值。

然而，Git 运行在实现层。一次提交记录了 `EnemyAI.cs` 被修改，并附有差异展示具体代码变更和描述*做了什么*的消息。但 Git 不捕获*为何*选择三阶段 Boss 结构而非替代方案、哪些证据支撑了该决策或该决策如何与项目更广泛的设计哲学相关联。设计推理——考虑过的替代方案空间、评估过的权衡、导向接受的推理过程——存在于 Git 的表征范围之外。

Git 也不区分探索性工作与已提交的决策。一个包含被放弃原型的分支和一个包含已接受实现的分支在 Git 模型中结构上完全相同。治理问题——哪项工作代表项目接受状态——与版本控制正交。

Workbench 不与 Git 竞争，而是在互补层上运行：项目的语义演进。Workbench 中的一个决策可能对应跨不同文件、仓库和时间段的多个 Git 提交。其关系为：Workbench 决定*为何*和*什么*；Git 记录*如何*。

### 3.2 架构决策记录（ADR）

架构决策记录\[15]提供了一种记录重大设计选择的结构化格式。典型的 ADR 包含决策的背景（问题与约束）、考虑的选项、选定的方案以及后果（正面与负面）。ADR 通常以 Markdown 文件形式存储于代码库旁，形成人类可读的架构选择历史。

ADR 与 Workbench 在决策溯源这一根本关注点上具有共性。它们认识到决策背后的*推理*往往比决策本身更有价值，记录此推理可使未来的参与者评估决策的前提是否仍然成立。

然而，ADR 在长期 AI 协作的背景下展现出若干局限性：

* **范围局限。** ADR 传统上针对架构决策——关于技术、结构和设计模式的、影响整个系统的选择。但项目涉及各层级的决策：功能设计、UI 文案、数值平衡、叙事方向、测试策略等。一个游戏项目每周可能做出数十项重要设计决策，其中大多数虽低于架构阈值，但对项目连贯性同样至关重要。
* **手动维护。** ADR 需要人工纪律来创建、更新、关联后续决策以及在适当时标记为已取代。在 AI 密集型工作流中，Agent 每次会话产生数十项提案，手动 ADR 维护变得不切实际。将每项决策以 ADR 格式记录的开销会淹没开发过程。
* **缺乏治理集成。** ADR 记录决策但不强制执行治理模型。没有机制阻止 AI Agent 在后续会话中将已被否决的替代方案视为既定事实。ADR 可能记录了血量缩放方案被否决，但如果 Agent 不读取 ADR——或读取了但不理解其权威性——该否决就没有任何效果。
* **无状态投影。** ADR 是叙述性文档，不生成机器可读的项目状态。回答"当前 Boss 战斗的已接受方案是什么？"需要阅读并解读多个 ADR，此过程易出错且不可扩展。

Workbench 沿三个维度扩展了 ADR 概念：范围（所有项目决策，而非仅架构级）、自动化（AI Agent 可提议决策并自动进入治理管道）以及集成（决策投影到项目接受状态，为"当前什么是真的？"提供确定性答案）。

### 3.3 事件溯源与 CQRS

事件溯源（Event Sourcing）\[16, 17]是一种架构模式，其中系统状态从不可变的事件序列派生，而非直接作为可变快照存储。当前状态是所有历史事件的*投影*，支持完整重建、审计和时序查询。如果投影损坏或需要新的查询模式，系统可以从事件历史重新计算投影。

命令查询职责分离（CQRS）\[18]通过将写模型（产生事件的命令）与读模型（为查询优化的投影）分离来补充事件溯源。此分离允许两侧独立优化、扩展和演进。

Workbench 从两种模式中汲取了重要灵感。决策（Authority Decision）→ 项目接受状态的关系映射了事件溯源的事件 → 投影模型：项目接受状态不是直接存储的，而是从所有已接受决策的投影派生。权威层（写侧，治理什么进入项目接受状态）与投影库（读侧，提供可浏览视图）之间的分离体现了 CQRS 原则。

然而，Workbench 在一个关键方面有别于传统事件溯源，这反映了项目治理的独特性质。传统事件溯源管理的是*系统状态*——订单、支付、用户注册、库存水平。这些是具有明确语义的数据变更。Workbench 管理的是*项目意义*——设计决策、职责分配、已接受的方向、演进中的愿景。一个断言"Boss 战斗系统使用三阶段"的权威决策不是数据库更新，而是一项治理行为，它改变了项目认为什么是真实的。

此区分具有实际意义。在传统事件溯源中，事件通常是追加且非矛盾的（创建订单、支付、发货；每个事件推进状态）。在 Workbench 中，决策可能冲突（新决策取代旧决策）、可能有条件（决策仅在特定情况下适用）、可能有争议（不同参与者可能对决策是否应被接受持不同意见）。治理模型必须在维持事件溯源核心属性——当前状态可从事件历史派生——的同时处理这些复杂性。

### 3.4 Agent 记忆系统

近期关于 LLM Agent 记忆的研究解决了跨交互维持上下文的问题。方法包括在上下文窗口内保留近期交互历史的会话缓冲区\[12]、基于语义相似度存储和检索相关过去交互的检索增强生成（RAG）系统\[19, 20]，以及将信息组织为不同抽象层级的语义类别的结构化记忆架构。

这些系统共享一个共同的优化目标：*召回*。它们旨在确保 Agent 在需要时能访问相关的过去上下文，提高 Agent 输出的质量和一致性。其潜在假设是：更多的相关信息导致更好的 Agent 性能。

Workbench 解决的是一个根本不同的问题。挑战不在于信息可用性，而在于*治理*：区分项目已接受为现实的信息与仅存在于项目历史中的信息。会话缓冲区以同等权重保留"Boss 应使用血量缩放"的提案和"Boss 使用三阶段"的最终决策。RAG 系统可能基于与查询的语义相似度检索到两者中的任何一个，不指明哪个代表当前已接受的方向。

Workbench 明确分离了这些类别。提案是提案（历史制品，无权威性）。决策是权威决策（接受状态，最高权威性）。未来 Agent 查询系统时，获得的不是相关段落的排序列表，而是结构化答案："Boss 战斗系统使用三阶段结构，由 2026-03-15 的决策 #52 确立，取代了决策 #19 的血量缩放方案。"

此外，Agent 记忆系统通常与特定 Agent 或模型耦合。当 Agent 更替时，记忆丢失或必须迁移——此过程存在信息丢失和不一致的风险。Workbench 的状态独立于任何特定 Agent，在设计上即可在参与者更替中存续。项目的记忆是项目的属性，而非任何参与者的属性。

### 3.5 Agent 框架

AutoGPT\[8]、ReAct\[9]、AutoGen\[10]、LangChain\[11]、CrewAI 等 Agent 框架专注于使 AI Agent 能够通过规划、工具使用和迭代优化来执行复杂任务。它们回答的问题是：\*Agent 如何完成目标？\*这些框架提供了任务分解、工具调用、自我反思和多 Agent 协调的机制。

这些框架假定项目上下文由外部维护。Agent 接收任务描述、使用可用工具执行任务、返回结果。框架优化的是任务执行的质量和效率，但不治理这些结果是否成为项目接受状态的一部分。

Workbench 作为互补层存在于 Agent 输出与项目状态之间，提供治理层。Agent 框架确保 Agent 能高效工作；Workbench 确保项目在 Agent 完成工作后保持连贯。两者的关系类似于 CPU 调度器（管理执行）与文件系统（管理持久状态）的关系。两者都是必要的，但单独都不充分。

### 3.6 变更管理与 ITIL

企业 IT 运营长期以来采用变更管理框架，特别是 ITIL\[21]，来治理对生产系统的变更。典型流程为：请求 → 审核 → 审批 → 实施 → 验证。这些框架解决了对生产系统的不受控变更可能导致中断、数据丢失或安全漏洞的风险。

Workbench 与变更管理在治理变更方面具有共同关注点，但焦点显著不同。传统变更管理治理的是*运维风险*："此服务器修改是否会导致停机？"Workbench 治理的是*项目意义*："此设计方向是否成为项目接受状态的一部分？"风险模型、审批流程和回滚机制不同，因为领域不同。

此外，Workbench 的治理模型是为 AI 参与的特定特征而设计的：高吞吐量提案、快速迭代以及对不同自动化级别的需求。传统变更管理假定人类驱动的工作流，变更频率相对较低，不能直接适用于 AI 密集型环境。

### 3.7 项目管理系统

任何熟悉软件工程的读者都会自然地问："Workbench 与 Jira、Linear、Notion 或 GitHub Projects 等项目管理工具相比有何不同？"这些工具在软件开发中无处不在，其区分很重要。

传统项目管理系统采用以任务为中心的模型：

```
任务 → 分配 → 状态 → 完成
```

它们回答的是：*谁在做什么？何时完成？当前状态如何？* Jira 通过工作流追踪工作项。Linear 以优先级和周期管理问题。Notion 提供灵活的文档和任务面板。GitHub Projects 将任务追踪与代码仓库集成。

这些工具在其设计用途上表现出色。但它们在长期 AI 协作方面有一个根本性局限：它们管理*工作*，但不管理*意义*。Jira 工单可能记录了"实现 Boss 战斗系统"在 Sprint 7 完成，但不记录*为何*选择三阶段结构而非替代方案、哪些证据支撑了该决策或何种设计哲学指导了该选择。

Workbench 采用以决策为中心的模型：

```
项目现实 → 决策 → 接受状态 → 任务分配 → 执行
```

它回答的是：*项目为何如此？什么被决定了？谁授权的？如果决策被证明有误，我们如何恢复？*

两者的关系是互补而非竞争的。项目管理工具追踪工作的*执行*。Workbench 治理工作的*意义*。一个良好集成的工作流可能使用 Workbench 进行决策治理，使用 Jira 进行任务追踪，并将 Workbench 决策关联到 Jira Epic。

### 3.8 近期趋同系统

近期出现的系统表明，这个问题空间正在趋同，而不是处于无人覆盖的真空中。PROJECTMEM 探索面向 Coding Agent 的本地优先、事件溯源记忆与判断层，包括类型化开发事件和行动前提醒。Agentic Engineering Framework 将任务门禁、分级审批、上下文预算和审计机制引入 Agent 工程工作流。Continuity Kernel 相关工作则聚焦于已接受状态的事务性激活，以及参与者更替过程中的显式谱系。SPM 及社区讨论中的其他项目记忆和连续性系统，也分别覆盖了相邻部分。这些名称在此作为社区可见的相邻实践提及，而不是正式文献引用；列表有意不求穷尽，也不应被理解为功能完备的比较。

这些系统是重要的近邻，而不是用来衬托的假想目标。它们说明项目记忆、连续性、溯源和治理正在成为相互关联的关注点。Workbench 探索的是一种特定整合：以头等 Project World 为中心，明确分离权威层 / 上下文层 / 执行层，采用提案 → 决策 → 接受状态管道，并支持跨软件、游戏、研究、写作及其他长期项目领域的参与者替换。因此，本文主张的是架构重点与组合方式，而不是声称不存在相关系统。

本文使用 **Continuity Harness（连续性支架）** 作为这一整合的描述性框架。它协调三种相关但有意分离的连续性：围绕任务分配、尝试、结果和任务局部 Handoff 的执行连续性；围绕项目视图与源制品的语义连续性；以及围绕提案、权威决策和项目接受状态的权威连续性。三条轴线可以相互作用，但上下文不会仅因为更新、更紧凑或由 Agent 生成，就自动成为项目事实。

### 3.9 差距识别

表 1 总结了现有技术的差距分析。

```
+---------------------------+------+-----+----------+-------+---------+-------+------+
| 能力                       | Git  | ADR | 事件源   | Agent | Agent   | PM    | WB   |
|                           |      |     | /CQRS    | 记忆  | 框架    | 工具  |      |
+---------------------------+------+-----+----------+-------+---------+-------+------+
| 实现追踪                   |  ✓   |     |          |       |         |       |      |
| 决策文档化                 |      |  ✓  |          |       |         |       |  ✓   |
| 任务追踪                   |      |     |          |       |         |   ✓   |      |
| 全范围治理                 |      |     |          |       |         |       |  ✓   |
| 从事件派生状态             |      |     |    ✓     |       |         |       |  ✓   |
| 读写分离                   |      |     |    ✓     |       |         |       |  ✓   |
| 上下文保留                 |      |     |          |   ✓   |         |       |  ✓   |
| Agent 无关状态             |      |     |          |       |         |       |  ✓   |
| 提案–决策治理              |      |     |          |       |         |       |  ✓   |
| Agent 可替换性             |      |     |          |       |         |       |  ✓   |
| 多层压缩                   |      |     |          |       |         |       |  ✓   |
| 权威溯源                   |      |     |          |       |         |       |  ✓   |
+---------------------------+------+-----+----------+-------+---------+-------+------+

表 1：现有技术差距识别。Workbench（WB）解决了治理、Agent 无关性与
项目连续性的交叉领域。PM 工具（Jira、Linear、Notion）管理任务但不管理意义。
```

Workbench 瞄准的是一个由不同系统以不同重点覆盖的交叉地带：（1）对各决策层级上构成项目现实的内容进行治理，（2）独立于特定 AI 参与者、模型和会话，以及（3）通过具有明确权威层级的多层信息架构实现长期连续性。Workbench 的贡献，是把这些关注点组合成一个围绕项目组织的统一契约，而不是一组松散功能。

此差距并非现有工具的失败。Git、ADR、Agent 框架、知识库和新兴的项目记忆系统各自在特定用途上发挥作用。开放问题在于：如何围绕一个持久的 Project World 整合治理、连续性和可替换性，同时不把系统变成另一个大而全的 Agent 平台。

```
              持久性
                  |
                  |
     Workbench  ★ |
                  |
          ADR  ●  |
                  |
       Git  ●     |
                  |
──────────────────┼──────────────────────────
           执行                    治理
                  |
                  |
  Agent           |          ●  PM 工具
  框架  ●         |
                  |
  记忆    ●       |
  系统            |
                  |
              瞬时性

图 6. Workbench 相对于现有工具的定位，沿执行 vs. 治理和
瞬时 vs. 持久两个维度。Workbench 占据持久–治理象限，
将长期状态维护与决策治理相结合。
```

### 3.10 领域驱动设计与限界上下文

领域驱动设计（DDD）\[22, 23]提供了另一个相关的理论基础。DDD 的*限界上下文*概念——明确定义的边界，特定领域模型在其中适用——与 Workbench 的层分离产生共鸣。权威层定义了项目现实的限界上下文；上下文层定义了项目理解的限界上下文；执行层定义了任务执行的限界上下文。

DDD 对*通用语言*——开发者与领域专家之间的共享词汇——的强调也影响了 Workbench 的设计。提案、决策、接受状态和取代等概念构成了项目治理的通用语言，在人类用户与 AI Agent 之间共享。

然而，DDD 是为具有人类开发者的软件系统设计的，而非为具有 AI 参与者的项目设计的。Agent 可替换性、自动化治理和压缩的挑战超出了 DDD 的范围。Workbench 将 DDD 的原则扩展到了这一新领域。

---

## 4\. 系统设计

本节介绍 Workbench 的核心设计。我们首先阐述驱动架构的设计原则，然后详细描述信息架构、形式化模型和系统组件。

### 4.1 设计原则

Workbench 的架构受五项设计原则约束，每项针对传统长期 AI 协作方法的特定失效模式。这些原则不是抽象的理想，而是直接塑造系统结构的工程约束。

**原则一：项目作为头等实体。** 项目——而非 Agent、会话或用户——是系统中的首要实体。用户、Agent、模型和工具都是项目的*参与者*。没有任何单一参与者等同于项目本身。

*动机：* 在传统 AI 辅助工作流中，项目身份隐式编码在一系列聊天会话中。当会话结束或 Agent 更替时，项目连贯性退化，因为没有独立于参与者持续存在的实体。通过将项目提升为头等实体，Workbench 确保项目具有独立于当前活跃参与者而持续存在的身份、历史和状态。

*架构含义：* 项目表示为具有自身身份的持久对象，独立于任何 Agent、会话或运行时。所有其他对象（Agent、会话、决策）与项目相关但不构成项目。

**原则二：权威分离。** 信息生成（由 AI 或人类执行）与项目状态接受严格分离。生成的输出是*提案*；只有明确的*权威决策*才能将提案转化为项目接受状态的一部分。

*动机：* AI Agent 生成看似合理但可能错误或不一致的输出。在没有权威分离的系统中，最新的 AI 输出可能覆盖既定决策，导致项目漂移。第六个月的 Agent 可能提出第二个月已被明确否决的方案，若无治理边界，该提案可能悄然成为项目接受状态。

*架构含义：* 系统强制执行严格的管道：提案 → 权威决策 → （可能的）接受状态。不存在捷径。无论 Agent 的角色或能力如何，它都不能直接写入接受状态。

**原则三：压缩不增加权威。** 当信息被压缩——摘要化、抽象化或蒸馏——压缩形式不获得凌驾于原始内容之上的权威性。摘要比原始会话更便于阅读，但不比原始内容更真实。

*动机：* 长期项目积累大量信息。压缩对于实际管理是必要的。然而，传统记忆系统往往将压缩信息视为等同于或优于原始信息，导致微妙的失真。一份不准确地描述某项决策的早期摘要可能被未来的 Agent 视为权威，尽管原始讨论包含摘要遗漏的细微差别。

*架构含义：* 每个压缩制品明确引用其来源，并携带权威标签表明它是派生的（上下文性）而非原始的（权威性）。系统强制执行层级：接受状态 > 权威决策 > 提案 > 上下文。

**原则四：Agent 可替换原则。** 系统不假定任何特定 Agent、模型或运行时的持久性。Agent 是可互换的参与者，其角色是产出提案和执行任务，而非体现项目。

*动机：* AI 领域变化迅速。模型被弃用，新 Agent 涌现，能力转移。将项目连续性绑定到特定 Agent 的系统在最不稳定的层创建了单点故障。Workbench 的设计确保 Agent 更替是运维转换，而非生存威胁。

历史记录支持此关切。在三年间（2023–2026），AI 领域见证了 GPT-3.5、GPT-4、Claude 1/2/3/4、Gemini、Llama、Mistral 以及众多专业编码 Agent 的出现与后续演进。任何将连续性绑定到此列表中特定模型的系统都需要多次迁移。Workbench 的可替换性原则确保此类迁移仅影响适配层，不影响项目核心状态。

*架构含义：* 所有 Agent 交互通过统一接口契约（任务分配 → 尝试 → 提案/Handoff）进行中介。Agent 的内部实现与项目连续性层无关。Agent 适配器处理此契约与 Agent 特定接口之间的转换。

**原则五：最小充分上下文。** 当新参与者需要理解项目时，系统应提供*最小充分*的信息视图。目标不是提供更多信息，而是在不悄然丢失重要约束的前提下，为当前任务提供足够信息。

*动机：* 上下文成本和认知负荷是真实约束。要求每次新会话重新阅读整个项目历史通常是浪费的，而肤浅的摘要可能导致误解。最小充分上下文是一项设计目标和阅读协议：先从紧凑的项目视图开始，在任务需要时再恢复更多细节。本文不把它表述为已经测得的词元节省结果。

*架构含义：* 系统维护多个语义视图（会话、Handoff、摘要层、投影库）以及具有权威性的接受状态，并提供根据任务复杂性选择适当视图的阅读协议。简单任务可以从接受状态开始；复杂任务可以检查决策和任务局部 Handoff；取证分析则回到原始会话。这些视图彼此相关，但 Handoff 不是接受状态的投影，上下文制品也不会因更简洁而获得更高权威性。

### 4.2 三层信息架构

Workbench 将所有项目信息组织为三个层级，每个层级具有不同的治理规则、数据类型和用途。此分离是使治理模型得以运作的架构基础。

```
                    人类用户
                        |
                        v
                  Leader Agent
                        |
            ---------------------------
            |                       |
            v                       v
     Worker Agents          治理层
     （按任务路由）         （提案 → 决策 → 状态）
            |                       |
            v                       v
      执行结果 -----------> 提案 ------> 权威决策
            |
            v
          Handoff
                                    |
                                    v
                         项目接受状态
                                    |
                    -------------------------------
                    |                             |
                    v                             v
            投影库投影               Git / 制品
            （只读视图）             （实现层）

      Handoff + 接受状态 ----------> 下一位参与者

图 2. Workbench 整体架构。Agent 产出执行结果和 Handoff。结果可以为提案
提供依据；提案进入治理层，由治理层决定是否进入项目现实。接受状态投影
到投影库（读取），Handoff 则为下一位参与者保留执行连续性。
```

```
┌─────────────────────────────────────────────────────────────────────┐
│                           项目世界                                   │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                       权威层                                  │  │
│  │                                                               │  │
│  │  提案 ──→ 权威决策 ──→ 项目接受状态                          │  │
│  │                                                               │  │
│  │  目的：定义项目认为真实的内容                                 │  │
│  │  规则：仅权威决策可修改此层                                   │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                              ▲                                      │
│                              │ 治理                                 │
│                              │                                      │
│  ┌───────────────────────────┴────────────────────────────────────┐  │
│  │                       上下文层                                  │  │
│  │                                                                │  │
│  │  会话 ──→ Handoff ──→ 摘要层 ──→ 投影库                      │  │
│  │                                                                │  │
│  │  目的：帮助参与者理解项目                                     │  │
│  │  规则：从属于权威层；不直接写入状态                           │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                       执行层                                   │  │
│  │                                                               │  │
│  │  任务分配 ──→ 尝试 ──→ 结果                                  │  │
│  │                                                               │  │
│  │  目的：记录工作如何被执行                                     │  │
│  │  规则：汇入上下文层；不直接写入权威层                         │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

图 7. 三层信息架构。权威层治理项目接受为现实的内容。上下文层提供
理解背景。执行层记录任务执行方式。数据向上流动：执行层为上下文层
提供信息，上下文层为提案提供信息，但仅权威决策定义项目接受状态。
```

**权威层。** 此层包含项目已接受的现实。其流程为：参与者（人类或 AI）产出一个**提案**（对项目的拟议变更）；一个**权威决策**决定该提案是否被接受；若被接受，变更进入**项目接受状态**。此层在某种意义上是不可变的：权威决策一旦做出，即构成永久的历史记录。项目接受状态是所有已接受决策的投影，而非可直接编辑的可变数据库。

权威层的不可变性对项目完整性至关重要。它意味着即使 Agent 犯了错误，该错误也被记录为提案（提议）而非状态变更。权威决策关口确保只有明确接受的变更才能进入项目接受状态。

**上下文层。** 此层保留帮助参与者理解项目的信息。**会话**捕获完整的交互历史，包括探索、死胡同和修正。**Handoff** 将会话的相关成果蒸馏给下一位参与者，提供结构化的工作交接。**摘要层**提供高密度项目上下文以供快速定位。**投影库**通过项目决策历史的投影视图提供长期导航和演进查看。

关键的是，上下文层中的信息*从属于*权威层。Handoff 可以包含未完成工作、拟议变更或待处理提案；声称"Boss 使用血量缩放"的摘要不凌驾于确立三阶段结构的权威决策之上。上下文制品会在必要时被标记为不完整、过时或不准确，但权威决策保持有效。此从属关系防止了一个常见失效模式：最新（或最简洁）的上下文文档被误认为项目事实。

**执行层。** 此层记录工作如何被执行。**任务分配**定义谁负责什么任务，包括任务的范围、目标和参考基线。**尝试**捕获执行过程，包括中间结果和迭代。**结果**记录产出了什么以及如何验证。

执行层汇入上下文层（执行结果产生 Handoff 并为理解提供信息），但不直接修改权威层（执行结果在成为项目接受状态之前需要治理）。此分离至关重要：完成实现任务的 Agent 产出了结果，也可能产生 Handoff，但两者都不会自动成为项目接受状态的一部分。拟议变更作为提案进入治理管道，等待权威决策。

**数据流。** 信息在各层之间向上流动：执行结果成为上下文，上下文为提案提供信息，提案经历治理，已接受的决策修改项目状态。反向流动——执行或上下文直接修改权威——被架构约束所禁止。

### 4.3 提案–决策–状态模型

提案–决策–状态模型形式化了变更如何进入项目已接受现实。该模型是 Workbench 的治理骨干。

**定义 1（提案）。** 提案 *C* 是对项目的拟议变更，包含：提议参与者（*p*）、拟议内容（*content*）、可选的支撑证据（*evidence*）和时间戳。提案可提议添加、修改或移除项目元素。

提案是所有变更的入口。无论由人类用户、Leader Agent 还是 Worker Agent 生成，每项拟议变更都作为提案进入系统。提案不携带内在权威性——它们是提议，不是事实。

**定义 2（权威决策）。** 权威决策 *D* 是一项治理行为，评估提案并决定其命运，包含：被决定的提案（*C*）、结果（*outcome* ∈ {接受, 拒绝, 延迟}）、授权实体（*authority*）、时间戳以及可选的对被取代的先前决策的引用（*supersedes*）。

权威决策是修改项目接受状态的唯一机制。一旦做出即不可变——决策本身不可更改，但可被后续决策取代。

**定义 3（项目接受状态）。** 时间 *t* 的项目接受状态 *S* 是 *t* 之前所有 `outcome = accept` 的权威决策的投影，按时间戳排序，冲突时以后续决策取代先前决策。

```
                    ┌─────────────┐
                    │   提案       │
                    │  （拟议中）  │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  权威决策    │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            |
        ┌─────▼─────┐ ┌───▼────┐ ┌────▼─────┐
        │  接受      │ │ 拒绝   │ │  延迟    │
        └─────┬─────┘ └────────┘ └──────────┘
              │
        ┌─────▼─────────────────────┐
        │  项目接受状态              │
        │  （所有已接受决策的投影）  │
        └───────────────────────────┘

图 3. 提案–决策–状态转移模型。提案是拟议的变更，
经历权威决策。只有被接受的提案通过投影进入项目接受状态。
```

**取代机制。** 当新的权威决策取代先前的决策时，先前决策保留在历史记录中但被标记为已取代。项目接受状态反映较新决策的内容。这使项目演进成为可能而不进行历史修正——完整的更替历史被保留，使未来的参与者能理解当前状态和产生当前状态的轨迹。

例如，若决策 #19 确立"Boss 使用血量缩放"而决策 #52 后来确立"Boss 使用三阶段"（取代 #19），项目状态反映三阶段 Boss，但历史记录保留血量缩放曾为已接受方案的事实，并解释了更改原因。

### 4.4 Leader-Worker 架构

Workbench 采用 Leader-Worker 架构进行 Agent 参与，通过明确的关注点分离，回应 Agent 在长期项目中所扮演的不同角色。

**Leader Agent。** Leader 是一个*状态解释者和提案协调者*——而非事实生成者。它作为项目与用户之间的主要界面。其职责包括：

* **状态理解：** 阅读项目接受状态、当前任务分配和相关上下文，理解项目的当前情况和进行中的工作。
* **任务分解：** 将用户目标分解为具体的、可分配的子任务，分派给具有适当能力的 Worker Agent。
* **任务分配创建：** 为 Worker Agent 定义明确的职责、目标和参考基线，包括作为工作基础的特定接受状态修订。
* **结果聚合：** 收集 Worker 输出，将其组织为连贯的呈现，并识别需要用户关注的决策点。

关键的是，Leader *不*拥有修改项目接受状态的权威。它可以提议（通过提案）但不能决定。它解释项目的当前状态并协调提案——它不生成事实。此设计防止 Leader Agent 的错误直接损害项目接受状态。

**Worker Agents。** Worker 执行由 Leader 分配的特定任务。每个 Worker 接收任务分配，使用其特定能力执行工作，并产出结果提案和 Handoff。Worker 可根据任务需求使用不同的模型、工具和运行时。

Leader-Worker 分离解决了一个根本性挑战：不同任务需要不同能力。架构推理受益于高推理模型；代码实现受益于专业编码 Agent；常规组织受益于轻量模型。Leader 将任务路由到最合适的 Worker，无需单一 Agent 处理所有职责。

### 4.5 Agent 路由与可替换性

Agent 可替换性是一等设计目标，而非事后考虑。系统必须在当前活跃的特定 Agent、模型或运行时变化时仍能正确运行。这通过统一接口契约实现：

1. **输入：** Agent 接收一个任务分配，包含任务描述、相关上下文（从适当的压缩层获取）以及对当前接受状态的引用。
2. **过程：** Agent 使用其特定能力（推理、代码生成、分析等）执行任务。
3. **输出：** Agent 以标准化格式产出结果提案和 Handoff，准备进入治理。

输出格式在各 Agent 间保持不变。无论提案由 GPT-5、Claude、Codex 还是本地模型生成，它都进入相同的治理管道：提案 → 权威决策 → （可能的）接受状态。

**Agent 适配器**层处理 Workbench 内部表征与不同 Agent 提供者特定接口之间的技术转换。适配器管理会话创建、任务交付、输出接收和 Handoff 转换——但明确不修改项目状态或覆盖权威边界。适配器是连接器，而非治理者。

此设计确保项目的连续性层与 Agent 生态系统正交。随着新 Agent 涌现和旧 Agent 弃用，仅适配层变化；项目的身份、决策和状态保持完整。

### 4.6 投影库作为投影层

投影库作为用户观察项目状态的主要界面。它明确*不是*第二个数据库，*不是*事实存储，*也*不是知识库。它是项目接受状态的只读投影，类似于 CQRS 系统中的物化视图。

投影库呈现：

* **时间线：** 权威决策的时间序列视图，展示什么被决定、何时、由谁、取代了什么以及哪些证据支撑了该决策。
* **演进视图：** 特定项目方面（如战斗系统、UI 设计、叙事结构）如何随时间变化，每次变更链接到其发起决策。
* **当前状态摘要：** 每个项目维度的当前项目接受状态，为"当前什么是真的？"提供快速参考。

投影库与权威层的关系映射了 CQRS 查询模型与命令模型的关系。信息单向流动：权威决策投影到投影库视图。反向流动——通过编辑投影库条目修改项目状态——被结构性禁止。此约束对维护治理边界的完整性至关重要。

### 4.7 轻量架构与本地优先设计

Workbench 刻意避免成为综合性 AI 平台。它不实现自己的 Agent 运行时、代码执行环境、云基础设施或知识管理系统。相反，它专注于项目连续性层，并将其他关注点委托给适当的现有工具。

**Workbench 是什么：**

* 项目身份层（这个项目是什么？）
* 决策治理层（决定了什么以及为什么？）
* 状态连续性层（当前已接受的现实是什么？）
* 可追溯层（我们如何到达这里以及如何恢复？）

**Workbench 不是什么：**

* IDE（委托给现有开发环境）
* 云平台（默认本地优先）
* Agent 运行时（委托给 Agent 框架和适配器）
* 知识库（知识提供上下文，不提供权威）
* 项目管理工具（追踪决策，不追踪任务）

本地优先设计确保项目核心状态驻留在用户处，而非可能被弃用的云服务中。这与项目持久性不应依赖基础设施持久性的原则一致。

SQLite 和 Git 的类比具有启发意义。SQLite 成功不是因为它是功能最丰富的数据库，而是因为它简单、可靠且自包含。Git 成功不是因为它提供了最多的项目管理功能，而是因为它优雅地解决了分布式版本控制这一个问题。Workbench 遵循相同的理念：彻底解决一个问题——面向 AI 协作的项目连续性——其余一切委托外部。

### 4.8 用户交互模型

以治理为中心的系统常有一个反复出现的关切：可用性——如果每次变更都需要明确的治理操作，用户是否花在治理上的时间比工作还多？本节描述 Workbench 的用户交互模型如何通过渐进式披露和隐式治理避免此陷阱。

**首次启动。** 用户首次打开 Workbench 时，体验刻意保持简单：

```
创建项目
  ↓
定义项目目标（一句话）
  ↓
Workbench 创建项目身份
  ↓
用户正常工作
```

用户不需配置治理策略、定义权威结构或学习系统术语。治理模型隐式激活：当用户做出重大变更（或 Agent 提议一项变更）时，系统将其捕获为提案。用户看到自然语言提示："此变更将影响战斗系统。接受？"而非："Claim #47 需要 AuthorityDecision。"

**持续工作。** 在正常工作期间，治理层大部分时间在不可见地运行：

```
用户说："把 Boss 改为五个阶段"
  ↓
Leader 解释为提案
  ↓
系统显示："Boss 战斗：3 阶段 → 5 阶段？"
  ↓
用户确认
  ↓
决策记录，接受状态更新
```

用户将其体验为确认对话，而非治理仪式。底层的提案 → 决策 → 状态管道在架构上存在，但在交互上是轻量的。

**治理发挥作用之时。** 治理模型仅在需要时变得可见：当变更与现有决策冲突时，当 Agent 提议先前已被否决的内容时，或当用户想理解项目为何处于当前状态时。这些时刻罕见但关键——系统在它们出现时清晰地呈现。

此设计确保治理成本与治理风险成正比。常规变更产生接近零的开销。重大变更产生少量确认成本。冲突变更产生明确解决的成本。用户不会对治理开销感到意外，因为系统根据变更的重要程度调整其介入深度。

---

## 5\. 信息流与上下文再理解成本

### 5.1 重新定义效率问题

在 AI 辅助开发中，效率通常被理解为单次交互层面的关注点。Workbench 将效率重新定义为*长期累积*层面的关注点：在整个项目生命周期中，由于反复再理解项目上下文而浪费了多少精力？

考虑传统方法。每次新的 AI 会话以有限的项目知识开始。用户必须重新解释目标、约束、先前决策和当前状态。对于跨越数十次会话的项目，此成本不断复合——不仅在词元层面，还在于每次重新解释引入不一致性的风险。

Workbench 的方法体现在两个原则中：

1. *理解一次。* 信息应至多被理解一次，以压缩形式存储，并在未来参与者需要时以适当的详细程度提供。
2. *仅存储增量。* 系统保留相对于前一层的变更，而非完整状态快照——类似于版本控制存储差异而事件溯源存储事件。

### 5.2 多层压缩模型

Workbench 使用五种相互关联的表征来支持渐进披露。它们不是单一的权威阶梯：会话、Handoff、摘要层和投影库支持语义连续性，而接受状态是具有权威性的项目状态。

```
表征              角色                    权威性      典型用途                可追溯性
────────────────────────────────────────────────────────────────────────────────────────
会话              完整交互日志            上下文性    取证恢复                完整
Handoff           任务局部检查点          上下文性    恢复进行中的任务        来源链接
摘要层            高密度项目视图          上下文性    快速定位                引用
投影库            只读投影                派生        长期导航                决策级
接受状态          项目现实                最高        治理当前工作            完整（通过链）
─────────────────────────────────────────────────────────────────────────────────

图 4. 相关上下文表征与权威边界。渐进披露描述参与者如何阅读信息，
并不意味着更紧凑的表征具有更高权威性。上下文表征不会获得凌驾于来源之上的权威性。
```

阅读协议遵循最小充分上下文原则：

```
新参与者加入项目
            |
            v
    阅读接受状态
            |
      充分吗？
       /        \\\\\\\\\\\\\\\_\\\\\\\_
      是           否
       |             |
    开始工作      阅读摘要层
                    |
               充分吗？
                /        \\\\\\\\\\\\\\\_\\\\\\\_
               是           否
                |             |
            开始工作      阅读决策链
                            |
                       充分吗？
                        /        \\\\\\\\\\\\\\\_\\\\\\\_
                       是           否
                        |             |
                    开始工作      阅读 Handoff
                                    |
                                 阅读会话

图 5. 压缩阅读协议。参与者从最充分的压缩层开始，
仅在需要时向下深入。
```

**会话。** 用户/Agent 与系统之间的完整交互历史。会话包含一切：探索、死胡同、修正、部分分析和最终输出。它们是最详细但最低密度的表征。会话是*交互期间发生了什么*的权威来源，但不是*决定了什么*的权威来源——决策必须通过权威决策明确做出才能进入项目的已接受现实。

**Handoff。** 由一次尝试或会话产生、供下一位参与者使用的任务局部连续性检查点。它可以包含任务边界、已完成的工作、产出、未解决问题、待处理提案、建议的后续检查，以及对相关接受状态元素的引用。Handoff 不是对话历史的复制，不是接受状态的投影，也不是权威记录。它是跨参与者边界恢复工作的主要机制；其适当大小取决于任务复杂度，本文不宣称经验性压缩比或词元节省。

**摘要层。** 项目当前状态、近期决策和进行中工作的高密度表征。摘要层是生成的制品——项目的压缩视图——因此不携带内在权威性。它们通过 ID 引用权威决策，而非将决策内容作为独立事实重述。设计良好的摘要层允许新参与者快速定位而不被误导将摘要本身视为事实。任何词元范围都只是说明性设计目标，而非实证结果。

**投影库。** 项目接受状态的只读投影，提供时间线视图、演进追踪和当前状态摘要。投影库是便于导航的表征，为快速参考而非详细理解而设计。投影库条目应足够简洁，以便高效浏览；任何词元范围都只是说明性设计目标，而非实证结果。

**接受状态。** 权威的项目接受状态，从所有已接受权威决策的投影派生。这是最高权威性的表征，详细程度取决于项目和任务。对于许多常规任务，仅阅读接受状态即可提供开始工作的充分上下文。接受状态回答"当前什么是真的？"而无需参与者理解其如何成为真的完整历史。

### 5.3 压缩中的治理

一个关键的设计关切是确保压缩不会引入治理失效。以下场景说明了此风险：

1. 会话 A 讨论了三种可能的 Boss 战斗方案：血量缩放、三阶段结构和自适应难度。
2. 会话被压缩为摘要层，提及全部三种方案但未明确指出哪种被接受。
3. 未来的 Agent 阅读摘要层并将"自适应难度"视为既定决策，而实际上仅"三阶段结构"被接受。

Workbench 通过两种机制防止此失效：

**权威标签。** 每个压缩层明确标记哪些信息源自权威决策、哪些是上下文推断。摘要层包含结构化引用："当前 Boss 设计：三阶段结构 \[决策 #52]。考虑过的替代方案：血量缩放 \[决策 #19，已取代]。"权威来源始终可追溯。

**取代追踪。** 当决策取代先前决策时，被取代的决策被标记为非活跃但保留在历史记录中。压缩层通过展示当前状态并链接到决策链来反映此点，而非静默省略被取代的内容。此确保即使在压缩视图中，完整的演进轨迹仍然可用。

### 5.4 长期成本分析

架构层面的预期是定性的：传统工作流会反复从对话摘录、文档、代码摘要和用户说明中重建项目上下文，而 Workbench 允许参与者先阅读紧凑的项目视图，仅在需要时再深入决策、Handoff 和会话。这可能减少重复再理解并改善上下文质量，但任何词元或时间收益的幅度都必须通过未来测量验证。

上下文再理解削减是次要收益，而非首要设计目标。首要目标是提供*更好的*上下文。接收到项目已接受决策及其溯源的清晰陈述的 Agent 将产出比接收到一堆过往对话的 Agent 更高质量的工作，即使总词元计数相似。

---

## 6\. 可追溯性、职责与可恢复性

### 6.1 职责的目的：恢复，而非追责

在 Workbench 中，职责概念服务于一个特定且有限的目的：在出错时实现恢复。职责不是关于为错误分配指责；它是关于维护溯源链，使系统能回答：*项目为何处于此状态，以及我们如何安全地改变它？*

此框架具有实际意义。当设计决策在采纳数月后被证明存在问题时，系统必须能够识别：

* **谁提议了该决策**（提案作者）——理解提案背后的视角和假设。
* **谁授权了它**（权威方）——确定谁承担了决策后果的责任。
* **哪些证据支撑了它**（证据引用）——评估决策的前提是否合理。
* **它取代了什么**（先前决策）——理解被放弃的内容及原因。
* **什么依赖于它**（受影响的项目状态）——评估潜在变更的影响半径。

此信息支持有针对性的修正而非全面回退。团队可以追溯有问题的决策到其源头，评估其对下游选择的影响，并做出取代它的新决策，同时保留无关的工作。

### 6.2 对象归属

Workbench 权威层中的每个对象都携带明确的归属信息，构成治理记录的一部分：

* **提案**归属于提议参与者（Agent 或人类），包括 Agent 身份、提案所在的会话以及驱动提案的任务分配（如有）。
* **权威决策**归属于授权实体，包括权威方是人类用户、自动化策略还是被委托的 Agent。
* **接受状态变更**归属于产生变更的决策，形成从当前现实回溯到治理行为再到原始提议的链。

此归属不仅是元数据，而是治理模型的组成部分。系统不仅能回答"当前 Boss 战斗结构是什么？"还能回答"谁提议了三阶段结构？"、"谁授权的？"、"之前的方案是什么？"以及"引用了哪些证据？"

对象归属还支持未来的治理精细化。例如，策略可规定某些 Agent 的提案需要人工审核，而具有良好记录的成熟 Agent 的提案可自动接受。归属链提供了评估和执行此类策略所需的信息。

### 6.3 通过取代机制实现可恢复的状态转移

取代机制是 Workbench 在不进行历史修正的前提下管理项目演进的主要工具。它使项目能够改变方向，同时保留其演进轨迹的完整记录。

当决策 #52 取代决策 #19 时：

* 决策 #19 保留在历史记录中，标记为已取代但不被删除。
* 项目接受状态反映决策 #52 的内容，仅投影活跃决策。
* 取代关系是明确的：决策 #52 记录它取代了 #19，#19 记录它被 #52 取代。
* 变更的推理（捕获在 #52 的支撑提案和证据中）与变更本身一同保留。

此设计支持多种恢复场景：

**部分回退。** 有问题的决策可被恢复先前方案的新决策取代，完整的更替历史被保留。

**影响分析。** 通过追踪哪些接受状态对象依赖于哪些决策，系统可在变更做出前识别潜在变更的影响半径。

**审计追踪。** 提案 → 决策 → 接受状态变更的完整链为项目演进的任何方面提供了完整审计追踪。

### 6.4 Agent 自主性与治理边界

一个关键设计问题是 Agent 在 Workbench 的治理模型中应拥有多少自主性。答案由一个结构性区分来约束：执行中的自主性，接受中的治理性。

**Agent 可自由地：**

* 提议任何提案，不论其质量或正确性。治理管道将根据其优劣评估该提案。
* 在分配范围内使用可用的任何工具、方法和方案执行任务。
* 产出包含详细结果、建议和支撑证据的 Handoff。
* 探索替代方案、开展分析和生成创造性提案，不受治理约束。

**Agent *不可*自由地：**

* 直接修改项目接受状态。无论角色如何，任何 Agent 都不能绕过提案 → 决策 → 状态管道。
* 覆盖现有权威决策。Agent 不能单方面推翻已正式接受的决策。
* 通过声称不具有的权威来绕过治理管道。

此分离支持可根据项目需求调整的自动化策略谱系：

**完全手动治理。** 每个提案在进入接受状态前需要明确的人工审批。这是高风险决策（架构变更、功能范围变更、设计哲学转向）以及治理模型尚在建立中的项目早期阶段的默认设置。

**基于策略的自动化。** 低风险提案（常规格式化、小幅修正、充分理解的实现任务）可基于预定义规则自动接受。高风险提案（架构变更、设计方向转向）仍需人工审核。策略本身是一项治理决策，必须明确做出并记录。

**混合治理。** Leader Agent 可被授权代表用户接受特定类别的提案，用户保留审核和推翻任何决策的能力。此模型适用于 Leader 已展示可靠性且用户在定义边界内信任其判断的项目。

治理模型是可扩展的：可在不修改核心架构的情况下添加新的授权策略，因为权威层的结构在所有策略下保持不变。

---

## 7\. 定位与设计哲学

### 7.1 Workbench 不是什么

对系统边界的清晰认知与对其能力的清晰认知同等重要。Workbench 明确不追求成为：

**不是 AI 聊天应用。** 聊天属于会话层（上下文层）。Workbench 治理从对话中产出的内容，不替代对话。

**不是通用 Agent。** Workbench 不进行推理、生成代码或执行任务。它治理结果，不治理执行。

**不是知识库。** 知识库回答"存在什么信息？"Workbench 回答"此项目决定了什么？"知识提供上下文；治理定义现实。

**不是项目管理工具。** PM 工具（Jira、Linear、Notion）追踪*任务*：谁做什么、何时。Workbench 追踪*决策*：项目为何如此。两者互补。

**不是执行平台。** 代码执行委托给 IDE、容器和编码 Agent。Workbench 治理*应该构建什么*，不治理*如何构建*。

**不是自动化决策黑箱。** Workbench 不做决策。它提供决策被做出、记录和追踪的结构。

### 7.2 适用性边界

Workbench 不具有普遍适用性。其价值在特定条件下最高，在其他条件下可忽略。

**适用于：**

* 跨越数月至数年的长周期项目，具有累积决策
* 涉及多个 AI Agent、模型或会话的工作流
* 预期发生模型迁移或 Agent 替换的项目
* 高上下文再理解成本（复杂领域、大型代码库）
* 需要可审计性或合规性的环境

**不适用于：**

* 一次性问答或单会话任务
* 小脚本、一次性原型或实验
* 单一稳定 AI 参与者且持续时间短的项目
* 决策琐碎或以接近零成本可逆的领域

拐点——治理成本被连续性收益证明合理的点——取决于项目时长、参与者数量和决策复杂度。粗略启发式：如果项目涉及超过 10 项重大决策和超过 20 次 AI 会话，Workbench 的治理开销可能是合理的。低于该阈值时，更简单的方法（会话记忆、手动笔记）可能已经足够。

### 7.3 与 AI Agent 安全的关系

Workbench 的范围在 AI Agent 安全方面是有意限定的。它不涉及对齐、模型能力控制或内容安全。这些是重要的关切，但它们属于 Agent 和模型层，而非项目连续性层。

Workbench 提供的是一个支持*通过溯源和可恢复性实现问责*的治理基底：将任何项目变更追溯到其授权决策、原始提议和支撑证据的能力，以及在变更被证明有问题时撤销或取代决策的能力。这不是对齐意义上的安全，而是组织可追溯性：回答*谁授权了什么、为什么、产生了什么后果*的能力。

此区分很重要。权威流程针对项目意义变化，而非所有执行行为。一个修复 Bug 的任务结果需要经过治理确认才能进入接受状态；而一次中间过程的截图或日志输出则不需要。此边界在实际使用中是否足够清晰，有待进一步探索。

对齐研究问："我们能否让 AI 行为正确？"Workbench 问："当 AI 参与项目时，我们能否追溯发生了什么并在出错时恢复？"这些是互补的关切，而非竞争的。一个治理良好但 Agent 未对齐的项目是有问题的；一个 Agent 对齐但缺乏治理的项目同样有问题。Workbench 解决后者。

此定位将 Workbench 置于负责任 AI 发展的更广阔图景中\[26]。它贡献了负责任 AI 的*透明度和可审计性*维度，而不声称解决*安全和对齐*维度。

### 7.4 与现有理论的关系

表 2 总结了 Workbench 与已建立的理论和实践基础的关系。

```
+------------------------+--------------------------------------------+-------------------------------------------+
| 理论 / 实践             | 核心贡献                                    | Workbench 的关系                            |
+------------------------+--------------------------------------------+-------------------------------------------+
| 事件溯源               | 从不可变事件派生状态                         | 采纳：权威决策 → 接受状态                   |
| \[16, 17]               |                                            | 映射事件 → 投影模型                         |
+------------------------+--------------------------------------------+-------------------------------------------+
| CQRS \[18]              | 读写模型分离                                 | 采纳：权威层（写）与投影库                  |
|                        |                                            | （读）显式分离                               |
+------------------------+--------------------------------------------+-------------------------------------------+
| Git \[14]               | 实现制品的版本控制                           | 互补：Git 追踪代码变更；                     |
|                        |                                            | Workbench 追踪决策演进                      |
+------------------------+--------------------------------------------+-------------------------------------------+
| ADR \[15]               | 架构决策文档化                               | 扩展：所有项目决策，不仅                     |
|                        |                                            | 架构级；与治理管道集成                       |
+------------------------+--------------------------------------------+-------------------------------------------+
| 变更管理               | 对生产系统变更的治理                         | 专门化：Workbench 治理项目                   |
| (ITIL) \[21]            |                                            | 意义，非运维风险；为 AI                      |
|                        |                                            | 密集型工作流设计                             |
+------------------------+--------------------------------------------+-------------------------------------------+
| 知识管理               | 信息存储与检索                               | 区分：知识提供上下文；                       |
| \[13]                   |                                            | Workbench 定义现实                          |
+------------------------+--------------------------------------------+-------------------------------------------+
| 领域驱动设计           | 通用语言、限界上下文                         | 受影响：项目身份和职责                       |
| \[22, 23]               |                                            | 模型反映 DDD 原则                           |
+------------------------+--------------------------------------------+-------------------------------------------+
| Agent 框架             | Agent 规划和执行能力                         | 互补：Agent 框架使能工作；                   |
| \[8-11]                 |                                            | Workbench 治理结果                          |
+------------------------+--------------------------------------------+-------------------------------------------+

表 2：Workbench 与现有理论和实践的关系。
```

Workbench 最好不被理解为对任何单一领域的新贡献，而是对已建立原则——事件溯源、命令查询分离、决策文档化、变更管理和领域驱动设计——的整合，应用于一个新的问题领域：涉及 AI 参与者的长期项目治理。

新颖性不在于任何单一技术，而在于特定的组合与应用：将事件溯源用于项目意义（而非系统状态）、将 CQRS 应用于决策治理（而非数据访问）、将 ADR 扩展到全项目范围（而非仅架构决策），以及将整个系统设计为支持 Agent 可替换性（而非 Agent 优化）。

---

## 8\. 实现状态

### 8.1 已验证组件

Workbench 架构已通过一个可运行的实现得到验证，表明核心治理模型不仅是概念性的，而且构成了一个可运行的系统基底。实现包含五个模块，每个独立测试：

|模块|通过测试数|失败|跳过|描述|
|-|-|-|-|-|
|Core|128|0|0|核心领域对象和治理原语|
|Runtime|63|0|0|运行时管理和会话处理|
|Project|28|0|0|项目身份和职责结构|
|Storage|307|0|0|持久化、检索和投影逻辑|
|App|496|0|0|应用层、UI 集成、工作流|
|**合计**|**1,022**|**0**|**0**||

表 3：Workbench 模块测试覆盖。所有测试通过，零失败。

此外，构建系统报告零错误和零警告，差异卫生检查通过。这些结果证实所提架构在内部一致且可实现。

### 8.2 组件状态

|组件|状态|描述|
|-|-|-|
|项目身份|已实现|独立项目身份，与 Agent 解耦|
|权威模型|已实现|提案 → 决策 → 状态治理管道|
|接受状态|已实现|基于投影的状态派生，源自决策历史|
|遗留边界|已实现|与 pre-Workbench 项目共存，不造成损坏|
|投影库投影|已实现|权威层到可浏览视图的只读投影|
|恢复与溯源|已实现|从当前状态到原始提案的完整可追溯性|
|产品交互层|已完成基础切片|面向用户的项目入口和手动工作流体验|
|Agent 参与层|已完成基础验证|已完成基础 Agent 接入验证，但 Agent 仍然只能作为参与者提交结果，不能直接修改项目真实状态。|
|问责层|部分实现 / 未来研究|已经有第一版证据记录，可以记录来源和 hash，但更复杂的证据（代码 diff、测试报告、多媒体）以后再做；委托链和影响传播仍属未来研究。|

表 4：当前开发阶段的组件实现状态。

### 8.3 当前阶段与后续步骤

当前阶段聚焦于从架构验证到产品体验的过渡。主要差距不在于架构——核心治理模型已验证——而在于体验：用户能否自然地创建项目、理解其状态、执行工作、看到变更被保留并返回继续工作？

开发遵循刻意排序的顺序：

**已验证基线。** 连续性内核、权威模型、手动项目工作流、Agent 参与适配器、证据溯源记录、重启恢复以及初步 WEIQI³ 跨 Agent 接力路径均已实现并经过运行检验。当前工作树快照报告五个模块共 1,022 项测试通过。

**当前完善。** 面向用户的 Agent 交互界面、交互细节、本地化覆盖和工作流易用性仍在持续调整。Agent 参与仍受边界约束：Agent 可以提交结果和提案，但不能直接修改项目接受状态。

**下一步验证。** 更广泛的 Provider 覆盖、可复现的公开打包、跨 Agent 接力演示、连续性 / Token 效率实验以及真实项目中的持续使用仍待验证。多人权威委托、影响传播和更深入的证据集成仍属于未来工作。

此排序是有意的：无论工作由人类手动完成还是由 Agent 执行，项目世界和治理边界都必须保持有效。Alpha 候选版本证明了这一基线；接下来的工作重点是让新用户能够轻松检查、操作并质疑这一基线。

### 8.4 开放研究与社区验证

Workbench 被呈现为一个开源架构实验。当前实现证明的是架构可行性，而非最终完成形态。系统的真正价值——即项目连续性机制是否能在长期真实项目中有效运作——需要在真实世界的时间跨度、参与者更替和决策累积中得到检验。

通过公开设计文档、实现状态和已知局限性，项目旨在探索以下问题：

项目连续性机制是否能够成为长期 AI 协作的实用基础设施；

治理模型在实际使用中需要何种程度的自动化；

用户能够接受多少显式确认而不产生治理疲劳；

多层压缩框架是否真的降低长期上下文再理解成本。

当前实现的目标不是建立行业标准，而是提出一个可验证的架构假设，并通过开源获得长期反馈。

---

## 9\. 讨论

### 9.1 治理成本与使用效率

Workbench 设计中最显著的权衡在于治理严谨性与运营效率之间。每个提案必须通过权威决策管道才能进入接受状态。这引入了摩擦：用户必须审核和批准变更，即使 AI 的提案可能正确。

此摩擦是有意为之的，但必须谨慎管理。如果每次微小的文本编辑都需要明确的治理审批，系统在常规工作中变得不切实际。相反，如果治理过于宽松，接受状态的完整性受损，系统退化为带额外开销的传统记忆系统。

拟议的缓解措施是*基于策略的治理*：按风险级别分类变更并施加适当的治理强度。常规低风险变更（格式化、小幅修正、充分理解的实现任务）可自动接受；重大变更（架构决策、功能范围变更、设计哲学转向）需人工审核。分类本身是一项治理决策，必须明确做出、记录，并随项目演进定期审视。

一个重要设计问题是治理策略由谁定义。在最简模型中，项目创建者在项目创立即定义策略。在更精细的模型中，策略本身也被治理——它们可通过与其他项目变更相同的提案 → 决策 → 管道被提议、辩论和修改。此元治理确保治理模型能随项目演进。

一个开放问题是治理成本是否对小型短期项目合理。Workbench 的价值主张对跨越数月或数年并涉及多个 AI 参与者的项目最为强烈。对于一次性任务或单 Agent 的短期项目，治理开销可能超过连续性收益。识别拐点——治理成本被连续性收益证明合理的点——是未来研究应解决的重要实证问题。

### 9.2 Leader Agent 可靠性

Leader Agent 在架构中占据关键位置：它是用户与项目之间的主要界面，负责任务分解、Worker 选择和结果聚合。如果 Leader 做出低质量的任务分解或选择不合适的 Worker，项目效率会下降——尽管治理模型防止了 Leader 损坏项目状态。

当前缓解依赖于治理边界：Leader 提议（通过提案），用户决定（通过权威决策）。这防止了 Leader 造成不可逆的损害，但不防止其浪费时间、产出低质量提案或做出次优路由决策。

未来研究应探索 Leader 评估机制，包括对照项目目标的自我评估、独立 Agent 的交叉验证和用户反馈集成。治理模型为此类机制提供了基础：由于所有 Leader 行为产出可追溯的提案，Leader 决策的质量可被回溯分析以识别优势和劣势模式。

### 9.3 采用阈值与用户摩擦

Workbench 引入的概念（提案、决策、权威、投影、取代）对大多数用户来说是陌生的。采用阈值——理解并开始使用系统所需的精力——是影响实际可行性的现实关切。

设计通过渐进式披露解决此问题：默认界面以自然语言展示项目状态（"Boss 战斗系统：三阶段结构，2026-03-15 接受"），而非系统术语（"AuthorityDecision #52, outcome: accept, supersedes: #19"）。用户可在需要时深入了解技术细节，但被不要求理解治理模型即可有效使用系统。

然而，治理模型的价值仅随时间推移才显现。创建项目并做出几项决策的用户不会立即体验到可追溯性和可恢复性的好处。价值在数月累积决策后复合——这意味着系统需要持续使用后其收益才可见。这带来了一个引导挑战：用户必须先投资系统才能体会其回报。

潜在缓解措施包括：（1）通过即时收益（如结构化项目组织和清晰的任务追踪）展示价值，而非仅靠长期连续性；（2）提供模板和引导工作流以降低学习曲线；（3）支持渐进式采用，用户可从简单的决策追踪开始，逐步使用更精细的功能；（4）提供导入机制，可从现有制品（Git 历史、设计文档、会议记录）引导项目，无需用户为每个过去的追溯创建正式决策。

导入机制对采用尤为重要。大多数用户不会从零开始使用 Workbench 启动新项目；他们会有已具有数月或数年历史的现有项目。导入此历史的能力——即使是简化或近似形式——通过允许用户在不付出从零开始的代价的情况下体验治理收益来降低采用阈值。

### 9.4 可扩展性考量

**多用户协作。** 当前设计解决单用户场景。扩展到多用户环境需要额外设计权威委托、组织层级、并发决策治理和冲突解决。Actor 模型提供了基础（每个用户是具有定义职责的 LogicalActor），但多用户权威策略的具体细节——谁可以提议？谁可以决定？冲突如何解决？——仍未探索。

**大规模项目。** 拥有数千项决策、数百名参与者和数年历史的项目将对压缩模型和投影库投影性能构成压力。多层架构为可扩展性而设计（参与者阅读最高密度的充分层，而非完整历史），但需要大规模实证验证。索引设计、查询优化和投影缓存在大规模下成为重要的工程关切。

**跨项目关系。** 实际开发通常涉及多个相关项目（如游戏与其引擎、产品与其文档、微服务套件）。Workbench 当前将每个项目视为独立实体。扩展模型以处理跨项目关系——共享决策、依赖追踪、协调演进——是一个重大的开放问题，可能需要项目之项目的抽象。

### 9.5 局限性与开放问题

Workbench 验证了架构可行性，而非长期有效性。当前评估验证了实现正确性（1,022 项测试，零失败）和架构一致性，但尚未通过受控用户研究确立生产力提升。我们坦诚承认以下局限。

**实现局限：**

* **不支持多用户。** 当前系统为单用户。多用户权威委托已设计但未实现。
* **基础 Agent 接入已完成。** 已完成基础 Agent 接入验证，但 Agent 仍然只能作为参与者提交结果，不能直接修改项目真实状态。完整的 Leader-Worker 集成和多提供者路由仍在后续工作中。
* **证据支持处于基础版本。** 已经有第一版证据记录，可以记录来源和 hash，但更复杂的证据（代码 diff、测试报告、多媒体）以后再做。
* **无自动化治理。** 基于策略的治理已设计但未实现。所有治理需要手动审核。
* **压缩质量未验证。** 多层压缩模型基于架构推理，非实证测量。
* **当前领域覆盖有限。** 当前实现和案例主要集中于软件与游戏开发；对研究、写作、内容创作及其他长期项目的适用性仍待展示。
* **无形式化验证。** 治理模型的正确性属性（如接受状态始终是已接受决策的有效投影）由测试验证而非形式化证明。
* **单项目范围。** 当前设计针对单个项目。跨项目治理（共享决策、相关项目间的协调演进）未被涉及。

**结构性失效模式：**

* **错误的权威决策。** 治理模型防止未授权的变更但不防止错误的决策。修正需要通过取代机制检测。
* **治理策略配置错误。** 自动化策略可能错误分类风险。校准需要领域专业知识。
* **压缩保真度损失。** 摘要层和 Handoff 可能微妙地扭曲原始决策。经过多个周期，扭曲会累积。
* **权威瓶颈。** 单用户设计创造了权威单点。不可用时项目停滞。

**实证验证局限：**

* **无用户研究。** 治理模型的实际可用性、交互自然度和认知负荷尚未经用户测试验证。
* **无长期数据。** 多层压缩在数年的项目演进中能否保持足够的语义保真度，仅有架构推理，无实证数据。
* **无真实多人治理。** 多用户权威委托、并发决策治理和冲突解决尚未在真实协作场景中验证。
* **Token 效率未量化。** 文中关于压缩降低再理解成本的论述基于架构推理，非实证测量。具体收益幅度有待验证。
* **治理疲劳未评估。** 用户能够接受多少显式确认操作而不产生疲劳，尚未通过可用性研究验证。

以上局限性是当前设计阶段固有的。它们代表了架构可行性与实际有效性之间的边界，有待通过社区验证和长期使用逐步弥合。

**未来工作的开放问题：**

* 用户在系统变得不切实际之前能容忍多少治理开销？
* 多层压缩在数年的项目演进中能否保持足够的语义保真度？
* 自动化权威策略应如何在不同项目类型间校准？
* 多 Agent 项目在异构模型下长期运行的行为如何？
* 治理成本被连续性收益证明合理的拐点在哪里？

---

## 10\. 未来研究方向

### 10.1 从连续性到问责

Workbench 当前的焦点是*连续性*：确保项目能在参与者更替中存续。下一个研究前沿是*通过溯源和可恢复性实现问责*：确保每个项目变更都能被完整解释、追溯和恢复。

连续性问："参与者更替时项目能否继续？"问责问："每个变更能否追溯到其起源、证据和授权？如果变更被证明有误，其影响能否被精确识别并安全逆转？"

从连续性到问责的转变需要多个领域的进展。决策溯源必须深化：每个接受状态变更不仅应可追溯到其授权决策，还应追溯到完整的提案、证据和推理链。影响分析必须开发：当决策被修订时，系统必须识别所有受影响的下游对象。因果链必须支持：系统应支持如"Boss 战斗系统为何如此结构化？"的查询，答案可遍历完整的溯源链。

### 10.2 证据完整性

当前已经有第一版证据记录，可以记录外部制品（文件、文档、测试结果）的来源和 hash。未来工作应在此基础上探索更丰富、可验证的证据引用——包括 Git commit 引用、CI 产物链接、测试报告归档以及其他复杂证据类型。证据引用将不仅包含指向制品的指针，还可包含可验证的完整性信息，以检测自引用以来证据是否被修改。

此方向也面临实际挑战：哈希的长期维护成本、跨工具集成的复杂性，以及不同证据类型对验证粒度的不同需求。因此，采用"可验证证据引用"而非"密码学完整性"的更宽泛框架，可能更具可行性。

### 10.3 权威委托链

多用户和多 Agent 环境需要委托机制。项目所有者可能将战斗设计权威委托给首席设计师，后者可能进一步将特定任务委托给 Worker Agent。委托链必须满足若干约束：委托不扩展超出委托者自身范围的权威；委托的权威可被撤销，且撤销传播到子委托；委托链本身是项目治理记录的一部分。

### 10.4 影响传播分析

当决策被做出或修订时，其影响可能以不立即明显的方式在项目中传播。战斗模型的变更影响敌人平衡、技能树、Boss 设计、UI 布局和测试计划。未来研究应开发正向传播机制（给定新决策，识别所有可能需要修订的下游对象）、反向传播机制（给定发现的问题，识别所有可能贡献的上游决策）以及传播策略（关于如何生成影响通知以及谁接收通知的规则）。

### 10.5 绑定语义世界与实现世界

Workbench 管理*语义*层（决策、职责、接受状态）。Git 管理*实现*层（代码变更、文件历史）。未来工作应建立这些层之间的正式绑定：Workbench 中的决策应可链接到实现它的 Git 提交，Git 提交应可追溯到驱动它的决策。此集成支持跨两层的查询："`EnemyAI.cs` 为何被更改？"追溯到决策 #52；"决策 #52 是否已完全实现？"检查对应的 Git 提交。

### 10.6 治理压缩

长期项目需要信息压缩，但压缩存在信息丢失或失真的风险。未来研究应开发*治理压缩*机制，确保：每个压缩制品引用其来源决策和提案；压缩制品明确标记哪些内容源自权威决策、哪些是上下文推断；系统支持验证压缩制品忠实地表征其来源。这是一个声称"Boss 使用三阶段"的摘要与一个声称"根据决策 #52，Boss 使用三阶段（取代决策 #19 的血量缩放方案）"的摘要之间的区别。后者保留了治理；前者掩盖了治理。

治理压缩还引发关于压缩策略的问题。谁决定会话何时应被压缩为 Handoff？谁验证 Handoff 忠实地表征会话的成果？在多 Agent 环境中，压缩应由产出原始内容的同一 Agent 执行，还是由独立 Agent 执行？这些问题将治理与更广泛的问责议程联系起来：压缩本身是一项治理行为，它塑造了未来参与者将了解的项目历史。

### 10.7 多模态证据与验证

当前 Workbench 实现聚焦于基于文本的证据：设计文档、代码注释、测试报告和讨论记录。未来工作应将证据支持扩展到多模态格式：图像（UI 模型、架构图）、视频（试玩录像、演示走查）、音频（会议录音、语音笔记）和结构化数据（分析仪表盘、性能基准）。

多模态证据为完整性验证引入了新挑战。文本证据可被哈希；图像和视频证据需要更复杂的验证机制。溯源链必须容纳本身是复杂工具链产物的证据（如由设计工具产出、导出为图像并作为布局决策证据引用的 UI 模型）。

### 10.8 跨项目治理

实际组织通常管理相关项目的组合。游戏工作室可能开发共享引擎的多个游戏；软件公司可能维护具有共享组件的产品套件；研究团队可能基于共同基础开展相关研究。

跨项目治理将项目连续性问题扩展到项目间层面。一个项目中的决策可能影响其他项目；共享组件需要协调治理；组织级策略可能约束单个项目决策。Workbench 当前的单项目模型不涉及这些关切，但治理原语（提案、决策、权威）为扩展提供了基础。

---

## 11\. 结论

本文提出了 Workbench，一种面向长期 AI 协作的项目连续性层架构方案。核心问题：当 AI 参与者随时间更替时，项目本身必须保持连贯存在。

Workbench 围绕共同的权威边界组合了三个机制。三层信息架构分离权威层、上下文层和执行层，旨在防止 AI 输出自动成为项目事实。提案–决策–状态模型管控哪些拟议变更进入项目接受状态。会话、Handoff、摘要层和投影库等上下文表征支持渐进披露并保留可追溯性，但不取代权威模型。

Workbench 不与 Agent 框架、知识库或版本控制竞争。它占据一个互补层，治理项目意义和决策来源。实现验证了架构可行性，但长期有效性尚待真实世界验证。局限性已坦诚承认：系统当前为单用户，基础 Agent 接入已完成验证，但仍不具备权威性，压缩模型有待实证验证。

Workbench 被呈现为一个开源架构实验。它的价值、可用性和长期影响仍是开放的实证问题，需要通过社区参与和持续实践检验。研究议程从连续性扩展到通过溯源和可恢复性实现的问责——从帮助项目在参与者更替中存续，到让变更更容易被解释、追溯和恢复。未来方向包括多人协作、可追溯性深化、开发工具集成以及社区驱动的持续验证。

Agent 会更替。工具会更替。参与者会更替。但项目本身应具备继续存在的能力——而这正是需要共同探索和构建的方向。

---

## 参考文献

\[1] M. Chen et al., "Evaluating Large Language Models Trained on Code," *arXiv preprint arXiv:2107.03374*, 2021.

\[2] J. Austin et al., "Program Synthesis with Large Language Models," *arXiv preprint arXiv:2108.07732*, 2021.

\[3] OpenAI, "GPT-4 Technical Report," *arXiv preprint arXiv:2303.08774*, 2023.

\[4] Anthropic, "The Claude Model Family," Technical Report, 2024.

\[5] S. Madaan et al., "Self-Refine: Iterative Refinement with Self-Feedback," *Advances in Neural Information Processing Systems*, vol. 36, 2023.

\[6] T. B. Brown et al., "Language Models are Few-Shot Learners," *Advances in Neural Information Processing Systems*, vol. 33, pp. 1877–1901, 2020.

\[7] A. Paranjape et al., "ART: Automatic multi-step reasoning and tool-use for large language models," *arXiv preprint arXiv:2303.09014*, 2023.

\[8] T. Richards, "AutoGPT: An Autonomous GPT-4 Experiment," GitHub Repository, 2023.

\[9] S. Yao et al., "ReAct: Synergizing Reasoning and Acting in Language Models," *International Conference on Learning Representations (ICLR)*, 2023.

\[10] Q. Wu et al., "AutoGen: Enabling Next-Gen LLM Applications via Multi-Agent Conversation," *arXiv preprint arXiv:2308.08155*, 2023.

\[11] H. Chase, "LangChain: Building applications with LLMs through composability," GitHub Repository, 2022.

\[12] P. Lewis et al., "Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks," *Advances in Neural Information Processing Systems*, vol. 33, pp. 9459–9474, 2020.

\[13] G. von Krogh, "Knowledge Management in the Stock Market: An Empirical Study of the Relationship between Knowledge Management and Stock Returns," *Journal of Knowledge Management*, vol. 13, no. 2, pp. 1–15, 2009.

\[14] L. Torvalds and J. Hamano, "Git: Fast Version Control System," 2005. \[Online]. Available: https://git-scm.com

\[15] M. Nygard, "Documenting Architecture Decisions," 2011. \[Online]. Available: https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions

\[16] G. Young, "Event Sourcing," in *Patterns, Principles, and Practices of Domain-Driven Design*, Wrox Press, 2015, ch. 14.

\[17] R. Richardson, "Pattern: Event Sourcing," *microservices.io*, 2018. \[Online]. Available: https://microservices.io/patterns/data/event-sourcing.html

\[18] G. Young, "CQRS," in *Patterns, Principles, and Practices of Domain-Driven Design*, Wrox Press, 2015, ch. 15.

\[19] Y. Gao et al., "Retrieval-Augmented Generation for Large Language Models: A Survey," *arXiv preprint arXiv:2312.10997*, 2023.

\[20] W. Zhong et al., "MemoryBank: Enhancing Large Language Models with Long-Term Memory," *Proceedings of the AAAI Conference on Artificial Intelligence*, vol. 38, no. 17, pp. 19724–19731, 2024.

\[21] ITIL Foundation, *ITIL 4 Foundation: ITIL 4 Edition*, AXELOS, 2019.

\[22] E. Evans, *Domain-Driven Design: Tackling Complexity in the Heart of Software*, Addison-Wesley, 2003.

\[23] V. Vernon, *Implementing Domain-Driven Design*, Addison-Wesley, 2013.

\[24] M. Fowler, *Patterns of Enterprise Application Architecture*, Addison-Wesley, 2002.

\[25] C. Rich and R. C. Waters, "The Programmer's Apprentice: A Research Overview," *IEEE Computer*, vol. 21, no. 11, pp. 10–25, 1988.

\[26] D. A. Norman, *The Design of Everyday Things*, Revised and Expanded Edition, Basic Books, 2013.

\[27] A. Cockburn, *Writing Effective Use Cases*, Addison-Wesley, 2001.

\[28] I. Jacobson, G. Booch, and J. Rumbaugh, *The Unified Software Development Process*, Addison-Wesley, 1999.

\[29] R. C. Martin, *Clean Architecture: A Craftsman's Guide to Software Structure and Design*, Prentice Hall, 2017.

\[30] G. Hohpe and B. Woolf, *Enterprise Integration Patterns: Designing, Building, and Deploying Messaging Solutions*, Addison-Wesley, 2003.

---

## 附录 A：实现映射

下表将本文定义的抽象概念映射到当前 Workbench 系统中的具体实现形式。

|概念|实现形式|存储模型|
|-|-|-|
|项目身份|Project 聚合根|SQLite 记录|
|权威决策|不可变决策事件|追加写入表|
|项目接受状态|投影服务|从决策日志派生|
|提案|Claim 仓库|版本化制品存储|
|Handoff|上下文制品|结构化 JSON 文档|
|摘要层|生成的上下文视图|读模型（可重计算）|
|投影库|只读投影|物化视图|
|任务分配|职责记录|关系表|
|逻辑参与者|Actor 聚合根|SQLite 记录|
|取消链接|决策关系|外键引用|

此映射供实现参考。本文的论点独立于任何特定存储技术；相同的架构可基于 PostgreSQL、Datomic、事件存储或基于文件的系统实现。
