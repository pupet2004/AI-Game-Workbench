# Continuity Harness 的理论基础

**状态：** Architecture Preview 的工作理论说明  
**日期：** 2026-08-28  
**范围：** 本文用于说明 Workbench 的理论血统与边界，不宣称下述收益已经被 Workbench 通过实证验证。

## 1. 定位

Workbench 更适合被理解为一个**面向长期 Agent 项目的连续性 Harness**。

这里的 Harness 指的是让 Agent 工作变得可检查、可重复和可恢复的周边结构：角色分离、任务路由、上下文选择、验证和反馈。Workbench 将这种思路扩展到项目层面，把项目作为持续存在的主体，而把 Agent、模型、会话和工具视为可以更替的参与者。

这是一种理论定位，而不是声称 Workbench 发明了 Harness Engineering。相关机制分别已有重要先例，包括 Agent 编排、上下文工程、渐进式披露、压缩和分层记忆。Workbench 提出的重点，是围绕受治理的项目状态对这些机制进行组合。

## 2. 连续性的三个维度

Workbench 将连续性看成三个相互关联、但不能混为一谈的维度：

### 执行连续性

当参与者或会话结束时，工作仍然可以继续。

`Assignment -> Attempt -> Result -> Handoff -> 下一位参与者`

Leader、Worker 和 Review 是角色分离执行的一种实现。Handoff 是任务局部的连续性检查点，记录做了什么、产出了什么、哪些地方仍不确定，以及下一位参与者需要检查或完成什么。

### 语义连续性

项目应该保持可理解，而不要求参与者重放所有历史交互。

`Library / 接受状态上下文 -> Summary -> Session / Source`

这表示渐进式披露关系，而不是事实权威等级。高层内容更密集、更容易扫描；低层内容保留更多细节与溯源。具体停在哪一层，取决于当前任务。

### 权威连续性

项目应该能够稳定回答：哪些内容已经被项目正式接受为真实？

`Claim -> Authority Decision -> Accepted Project State`

这部分不应被并入 Memory。上下文可以帮助参与者理解项目，但只有权威决策可以修改项目接受状态。

## 3. 两条正交轴

语义结构和权威结构是相互独立的：

| 轴 | 主要问题 | Workbench 结构 |
| --- | --- | --- |
| 语义连续性 | 当前任务需要哪些信息才足够？ | Library、Summary、Handoff、Session、Source |
| 权威连续性 | 哪些内容被正式接受为项目现实？ | Claim、Decision、Accepted Project State |

Library 可以比来源更紧凑，但不因此拥有更高权威。Handoff 可以更适合恢复一个任务，但不因此成为项目事实。保持两条轴分离，是 Workbench 的核心约束之一。

## 4. 最小充分上下文

Workbench 将 **Minimum Sufficient Context（最小充分上下文）** 作为上下文选择原则：

> 对于给定任务，提供能够保留足够决策相关信息、支持下一步行动的最小上下文集合。

这是一项优化目标和阅读协议，不是已经测得的 Token 节省结果。参与者可以先阅读高密度项目视图，只有在当前任务需要更高保真度时，才继续深入决策、Handoff、会话或源文件。

这条原则不意味着上下文越少越好。删除信息可能造成歧义、隐藏不确定性或遗漏例外。因此，“充分”必须结合任务和所需的信心水平来判断。

## 5. Handoff 与 Summary 的语义

### Handoff：任务局部连续性检查点

Handoff 不是对话历史的复制，也不是权威记录。它是一种结构化的执行制品，可以包含：

- 尝试的任务及其范围；
- 已完成和未完成的工作；
- 结果与支撑证据；
- 待处理或拟议的 Claim；
- 未解决的问题与风险；
- 相关 Accepted Project State 元素的引用；
- 建议的下一步；
- 指向来源 Session 或源文件的链接。

Handoff 的适当大小取决于任务复杂度。这里不预设固定压缩比。

### Summary：面向决策的语义压缩

Summary 不是为了复述 Session 中发生的每个事件。它应保留未来参与者真正可能需要的信息：决策、约束、被拒绝的替代方案、重要失败、未决问题，以及重大变化的原因。

Summary 仍然是派生上下文。它应该引用来源，不能因为更短或更容易阅读，就默默获得权威。

## 6. 与既有工作的关系

Workbench 借鉴了几条已有脉络：

- **Agentic Harness Engineering：** 角色分离、编排、验证、反馈和可强制执行的边界；
- **Context Engineering 与渐进式披露：** 将上下文视为有限资源，选择高信号信息，并在需要时取回细节；
- **Compaction 与 Handoff 控制：** 在参与者边界减少或过滤既有历史；
- **Hierarchical Memory：** 按抽象层级组织信息，并在适合当前任务的层级检索。

这些先例支持单个机制的合理性，但并不单独证明 Workbench 的整体架构或长期收益。

Workbench 的架构重点，是把这些机制与**权威连续性**结合起来：上下文记忆帮助参与者工作，而 Accepted Project State 定义项目正式接受的内容。

## 7. 工程原则：仅存储增量

“Store only the delta（仅存储增量）”是 Workbench 的工程原则，不应归因于某一个既有系统。它综合了结构化进度记录、决策日志、压缩和渐进式披露：

- 保存持久性的变化及其原因；
- 保留高保真来源供检查；
- 不把重复陈述当成新的权威；
- 当增量不足时，允许未来参与者取回细节。

如果真实项目表明其他保留策略能带来更好的恢复效果或语义保真度，这条原则也应当被修订。

## 8. 待验证假设

以下是未来需要验证的假设，不是当前研究结论：

1. `Handoff + Accepted Project State` 可能让新参与者比冷启动会话少重放一部分历史。
2. 权威分离的上下文可能减少意外的决策漂移。
3. 面向决策的语义压缩，可能比通用对话摘要保留更多决策相关信息。
4. 角色分离的执行可能提高可审查性和可恢复性，即使不提高原始任务速度。
5. 随着项目时长、参与者更替和累积决策增加，连续性机制的价值可能上升。

这些假设需要受控比较、真实项目使用和纵向证据。

## 9. 边界

Workbench 不声称发明了 Agent Harness、分层记忆、上下文工程或 Compaction，也不声称 Accepted Project State 能解决项目正确性、Agent 对齐或组织治理问题。

更窄的架构主张是：长期项目可以同时拥有执行结构、语义上下文结构和权威结构，并在它们之间建立连接，同时避免上下文制品静默地变成项目事实。

## 选读

- OpenAI：*Harness engineering: leveraging Codex in an agent-first world*；
- Anthropic：*Effective context engineering for AI agents*；
- OpenAI Agents SDK：*Handoffs*；
- *MemGPT: Towards LLMs as Operating Systems*；
- *RAPTOR: Recursive Abstractive Processing for Tree-Organized Retrieval*；
- H-MEM 与 HiGMem 关于长期 Agent 分层记忆的近期工作。

这份选读是相邻思想的地图，不是穷尽性的文献综述，也不表示这些工作与 Workbench 完全等价。
